# 2026-09-03

## 移除 tech 模块「工艺编制」动态方案功能
- `frontend/src/components/tech/board.vue`：删除模板中的「工艺编制」section（原 459-464）、`planComponentMap` / `currentPlan` / `planModel`、二级工艺 watch、`onCancel` 中的 plan 重置；保留一级工艺变更清空二级工艺的逻辑。
- `frontend/src/components/tech/draft.vue`：同步清理（它同样引用了 `./plan/*`，且被路由 `/web/tech/draft` 加载，不清理会导致构建失败）。
- 删除 `frontend/src/components/tech/plan/` 下全部子文件（19 个方案组件 CHTZ/TH/ZH/TP*.vue + types.ts），目录保留为空。
- 注意：`board.vue` 仍存在两处既有 TS 警告（未使用的 `craftTree1`、`onCancel`），非本次改动引入。

## board.vue 新增「工艺编制」工序动态表格
- 列：序号 / firstLevel / secondLevel / step(工序名称) / stepCode(工序编号 S01…) / sort / isNeed / remark / 操作，行内可编辑。
- `initStepRows()`：按当前一、二级工艺调 `techStepAPI.search({firstLevel,secondLevel,page:1,pageSize:200})` 初始化；无数据或失败时补一行空白；`watch(basicForm.secondLevel, initStepRows, {immediate:true})`，一级工艺变更清空二级也会自动触发重新初始化。
- 字段映射约定：后端 techstep 表 `step`=工序编号、`stepName`=工序名称；本表格 row 的 `step` 存名称、`stepCode` 存编号。
- 工序下拉数据 `stepMap`（S01-S20）与 `step.vue` 保持一致；选择 stepCode 后由 `onStepCodeChange` 自动带出 step 名称。
- 工艺编码→名称映射 `stepFirstLabelMap` / `stepSecondLabelMap`：先铺 `craftTree1`（完整树，与 step.vue 一致）再用 `craftTree`（board 自有 CH/ZH 树）覆盖，未命中则回显编码。

## board.vue 工序表「排序」列改为数字输入
- `StepRow.sort` 类型 `string` → `number | null`；`createStepRow` 直接赋 `stepRows.value.length + 1`。
- 新增 `strToNum(v)` 辅助函数（与既有 `numToStr` 配套），`initStepRows` 中 `sort: strToNum(r.sort)` 做后端字符串→数字转换。
- 表格列：`<el-input>` → `<el-input-number :min="1" :precision="0" size="small" controls-position="right" style="width:100%">`，列宽 90 → 110。
- 顺带删除了重复的第二个 `prop="sort" label="排序"` 列（原 595-599，与 566-570 完全重复）。
- 提交入参 `TechBoardSaveDTO` 目前尚未包含 stepRows，故类型变更不影响保存逻辑。

## board.vue 工序表新增行内「保存（暂存）」+ 只读锁定
- `StepRow` 增加 `saved: boolean`；新增 `stepStagedRows = ref<StepRow[]>([])` 作为暂存区，保存时 push 的是**行对象引用**（非拷贝），便于编辑/删除时按引用反查移除。
- 操作列宽 80 → 140：未暂存显示「保存」，已暂存显示「编辑」（取消暂存）+ 始终显示「删除」。
- `handleSaveStep(row)`：校验 stepCode 非空 → push 暂存区 → `row.saved = true`；`handleEditStep(row)` 反查移除并解锁；`handleDeleteStep(index)` 同步清理暂存区。
- 行内 `sort` / `step` / `stepCode` / `isNeed` / `remark` 五个单元格统一加 `:disabled="row.saved"` 实现只读。
- 重新初始化（`initStepRows`，含二级工艺为空的分支）与 `onCancel` 均清空 `stepStagedRows`，避免暂存区残留脏数据。

## board.vue 工序表按 sort 正向排序
- 新增 `sortStepRows(rows: StepRow[])`：`[...rows].sort((a,b) => (a.sort ?? MAX_SAFE_INTEGER) - (b.sort ?? MAX_SAFE_INTEGER))`，即按排序号升序，sort 为空/null 的行排末尾（Array.prototype.sort 稳定，排序号相同保持原顺序）。
- `initStepRows()`：有数据走 `sortStepRows(list.map(...))`，无数据（或加载失败）仍补一行空白 `createStepRow`；`stepRows` 为 ref，赋值后 el-table 自动同步刷新。
- `handleAddStep()` 改为 `stepRows.value = sortStepRows([...stepRows.value, 新行])`，新增行按排序号归位；行对象引用不变，暂存区 `stepStagedRows` 不受影响。
- 排序号输入框未做输入时实时重排（避免编辑跳行），需要重排可点「重新加载」。

## board.vue 工序表「重新加载」改为纯内存「排序」
- 用户要求：重排只针对内存中已有的 `stepRows`，不重新请求后端、不清空 `stepStagedRows`（行对象引用不变，暂存/只读状态不受影响）。
- 新增 `handleSortStep() { stepRows.value = sortStepRows(stepRows.value) }`；工具栏按钮 `重新加载` → `排序`，`@click` 由 `initStepRows` 改为 `handleSortStep`。
- `initStepRows()` 保留：仍由 `watch(basicForm.secondLevel)` 触发，从后端加载工序模板（该场景替换整表，清空暂存区仍属正确）。

## 输出今日工作日志
- 用户要求把当天工作日志导出到 `docs/today.md`（该目录原先不存在，已新建）；内容为当日 memory 日志按主题整理后的 6 个条目 + 待办/遗留。

## 输出本周工作周报 docs/20260903.md
- 周报覆盖 2026-08-31（周一）~ 09-03（周四），周五未结束；08-30 的 1 次提交未计入。
- **日志位置（重要）**：本仓库有三处 `.codebuddy/memory` —— 根 `f:\hb\page\.codebuddy\memory`（仅 09-03）、`frontend\.codebuddy\memory`（08-29 ~ 09-02 + MEMORY.md，前端工作主日志）、`backend\.codebuddy\memory`（08-22 ~ 09-02 + MEMORY.md，记录的是真后端 F:\hb\api 的工作）。写周报/日志时三处都要读，不能只看根目录。
- 周报结构：概览表 → 按领域详述（部署/工程化、类型质量、工作流审批、工艺编制、工序流程、复用精简、后端）→ 按日时间线 → 13 条沉淀约定 → 遗留风险与下周计划 → 变更文件清单。
- 本周主线：部署形态定案（hcs.war + api.war 同 Tomcat 同源）、vue-tsc 44 错清零、工艺编制从 19 个方案组件切换为工序动态表格、后端 tool 包/JWT/Mapper 缺陷修复 + TechStep 全栈落地。

## 建立工作区结构文档 docs/structure.md
- 用户明确约定：**`frontend/` = 前端页面根目录，`backend/` = 后端接口根目录**，据此新建 `f:\hb\page\docs\structure.md` 固化全仓结构。
- 文档内容：顶层布局表（frontend/backend 技术栈、产出 war、context-path）、frontend 根目录文件 + `src/` 完整树（api 17 模块 / router / config / utils / components 9 个业务目录）、22 条业务路由→组件→`meta.right` 权限码映射、backend 分层（controller 18 / service 17 / repository 16 / mapper 9 / entity 16 / dto 10）、5 大业务模块、跨端协作要点 4 条。
- 顺带纠正旧记忆：之前记的「F:\hb\page\backend 是废弃 Fastify 原型」**有误**，实际有 107 个 java 文件，就是真后端（Spring Boot），已在 update_memory 中修正。
- 发现的过时文档（未改，仅记录）：`frontend/CODEBUDDY.md` 与 `.env.production` 注释仍称后端为 Fastify；`frontend/docs/struct.md` 描述的 `api/base.ts`、`data/data2.vue` 已不存在。
- 新建 `f:\hb\page\.codebuddy\memory\MEMORY.md`（长期记忆），收录结构约定、技术栈部署、接口前缀/ddl-auto/受保护文件等约定、三处 memory 分布、用户偏好。

## 后端主本确认（用户 2026-09-03 确认）
- **`F:\hb\page\backend` 是唯一主本**，所有后端改动在此进行。
- **`F:\hb\api` 已不存在**，后续不再参考该路径（此前记录的「两者择一」歧义已消除）。

## 部署形态定位 + 测试阶段一键发布脚本
- 用户确认：前后端两个 war 同在一个 Tomcat（`/hcs` + `/api`）**只是临时方案，后期会调整**；测试阶段要简化发布流程。
- 新建 `f:\hb\page\deploy-test.ps1`（参数 `-Part All|Front|Back` / `-NoBuild` / `-NoTail` / `-Force` / `-Tomcat`），固化 build.md 的 5.1~5.4：增量构建（后端 `package` 不带 `clean`）→ `tar -tf` 校验 hcs.war 目录条目与 WEB-INF/web.xml → shutdown 等 java 退出（30s，默认不强杀）→ 清理 webapps+work 中本次目标 app → 拷 war → startup → 自动跟踪最新 catalina*.log。
- `-Part Front` 是高频路径：只重打前端，避免每次动 66MB 后端包。
- 已用 `-NoBuild -NoTail -Tomcat C:\__nonexistent__` 验证脚本可解析执行（中文输出正常，无需 BOM 转换），错误分支按预期抛出。
- `frontend/docs/build.md` 同步更新：顶部加「部署形态是临时的」警示 + 指向一键脚本；全文 `F:\hb\api` → `f:\hb\page\backend`（14 处）；新增「5.0 测试阶段：一键发布」小节；修正第十节「backend 是废弃 Fastify 原型」的错误条目。

## 新建独立演示页 frontend/src/components/solo.vue
- 需求：10 个 div 区块，块间距 10px，每块含标题栏 + 内容区，标题右侧有折叠/删除按钮；折叠控制内容显隐，删除隐藏整块。
- 实现：纯 Vue3 + Element 无关的原生 CSS（箭头用 border 三角、删除用 ::before/::after 交叉线），不依赖 Element Plus 图标，减少注册依赖；容器 `display:flex; flex-direction:column; gap:10px` 保证间距；`Panel` 接口含 `open`/`removed`，用 `v-show` 而非 `v-if` 保留状态。
- 未注册路由，也未加入菜单（用户未要求）；需要预览时在 `src/router/index.ts` 加一条 `web/solo` 子路由即可。

## solo.vue 注册路由 + 接入菜单
- 路由：`src/router/index.ts` 的 `/web` children 末尾新增 `{ path:'solo', name:'Solo', component: () => import('../components/solo.vue') }`。**故意不写 `meta.right`**——守卫里 `need && !hasRight(need)` 才拦截，无 right 则登录即可访问（演示页不需要权限码）。
- 菜单：`src/config/menu.json` 顶级追加 `{ "name":"独立演示", "path":"/web/solo", "icon":"Grid" }`，同样不带 `right`，故 `filterMenu` 对无 right 的叶子项始终放行，任何人登录都可见。
- 菜单图标名必须是 `@element-plus/icons-vue` 里的组件名（已核实 `Grid` 存在于 `node_modules/@element-plus/icons-vue/dist/types/components/grid.vue.d.ts`）；`MenuBar` 的 `iconComp` 取不到时回退 `Document`。

## solo.vue 改为模板平铺（去掉 v-for）
- 用户明确要求：**不用 v-for 构建区块，改为 10 个 div 平铺**，原因是后期每个 div 内要各自填充不同的表单/表格，循环写法不利于逐块定制。
- 折中方案：DOM 结构在模板里完整平铺 10 份（每块带 `<!-- ---------- 区块 N ---------- -->` 分隔注释和 `<!-- TODO -->` 占位），**只有 `open`/`removed` 两个开关状态仍集中在 `reactive<BlockState[]>` 的 `blocks` 里**，避免 20 个散落的 ref。
- `toggle(index)` / `remove(index)` 用 **1 起的区块号**入参，内部 `blocks[index-1]` 取状态，模板里 `@click="toggle(1)"` 与标题「区块 1」数字对齐，避免 0/1 基数错位。
- 后续要加业务：在对应 `.panel-body` 里直接写表单/表格，各自的 ref/model 独立声明，不要合并进 `blocks`。
