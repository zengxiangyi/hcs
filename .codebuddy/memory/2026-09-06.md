# 2026-09-06

## 待办页面类型对齐修复
- 后端 `/workflow/todo` 实际返回 `PageResult<common.Todo>`，JSON key 为：workflow/flowgraph/flownode/starttime/remark/nodeName/operator/rolelist/userlist/code/name/category/targetcode/sender/beginTime/state（出现 `starttime` 与 `beginTime` 两个时间字段）。
- 前端原用 `WorkflowRow`（`startTime`/`flowGraph`/`id`）导致「发起时间」「流程图」列空白、审批调用传错 `flowGraph`。
- 修复：`frontend/src/api/workflow.ts` 新增 `TodoRow` 接口与 `TodoListResult`，`todo` 返回类型改 `PageResult<TodoRow>`；`frontend/src/components/approval/todo.vue` 改用 `TodoRow`，列绑定改 `beginTime`/`flowgraph`，新增「当前节点」(`nodeName`) 列，移除不存在的 id 列，审批调用 `flowGraph` 改 `row.flowgraph`。改动需 `npm run build:war` 重新打包前端才生效。

## 待办表格显示按 Todo 接口格式重排
- `frontend/src/components/approval/todo.vue` 表格列对齐 `TodoRow`：删除隐藏的 `flowgraph` 占位列（数据仍在 row 上，`submitApprove` 仍读 `row.flowgraph`），新增「节点等待时间」(`starttime`) 列。列序：任务编号(code)/任务名称(name)/分类(category)/当前节点(nodeName)/发起人(sender)/发起时间(beginTime)/节点等待时间(starttime)/状态(state)/备注(remark)/操作。改动需重新 `npm run build:war` 前端才生效。

## 待办属性名对齐 Todo.java（camelCase 定稿）
- 用户明确：前端 `TodoRow` 与表格列须按后端 `common.Todo` 的 Java 属性名（camelCase）对齐，不是 DB 列名的小写形式。
- `backend/.../common/Todo.java` 属性：`workflow/flowGraph/flowNode/startTime/remark/nodeName/operator/roleList/userList/code/name/category/targetCode/sender/beginTime/state`。Mapper `TodoMap` 用 `property="flowGraph"` 等映射，Jackson 序列化为 camelCase JSON key。
- 修正：`frontend/src/api/workflow.ts` 的 `TodoRow` 由小写（flowgraph/flownode/starttime/rolelist/userlist/targetcode）改为 camelCase（flowGraph/flowNode/startTime/roleList/userList/targetCode）。
- 修正：`todo.vue` `submitApprove` 的 `row.flowgraph`→`row.flowGraph`；表格 `prop="starttime"`→`prop="startTime"`；顺手修复 `el-table-colun` 拼写错为 `el-table-column`（其 `prop="targetCode"` 本就正确）。
- 注意：**之前日志里「后端返回小写 key（flowgraph 等）」是错的**，系误把 DB 列名当 JSON key；实际应以 Todo.java 属性名（camelCase）为准。改动需重新 `npm run build:war` 前端才生效。
