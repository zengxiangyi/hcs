# 2026-09-06

## 待办页面类型对齐修复
- 后端 `/workflow/todo` 实际返回 `PageResult<common.Todo>`，JSON key 为：workflow/flowgraph/flownode/starttime/remark/nodeName/operator/rolelist/userlist/code/name/category/targetcode/sender/beginTime/state（出现 `starttime` 与 `beginTime` 两个时间字段）。
- 前端原用 `WorkflowRow`（`startTime`/`flowGraph`/`id`）导致「发起时间」「流程图」列空白、审批调用传错 `flowGraph`。
- 修复：`frontend/src/api/workflow.ts` 新增 `TodoRow` 接口与 `TodoListResult`，`todo` 返回类型改 `PageResult<TodoRow>`；`frontend/src/components/approval/todo.vue` 改用 `TodoRow`，列绑定改 `beginTime`/`flowgraph`，新增「当前节点」(`nodeName`) 列，移除不存在的 id 列，审批调用 `flowGraph` 改 `row.flowgraph`。改动需 `npm run build:war` 重新打包前端才生效。
