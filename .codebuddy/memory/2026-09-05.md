# 2026-09-05

## CODEBUDDY.md 重构为渐进式披露结构
- 三份 CODEBUDDY.md（根/frontend/backend）均改为「索引 + 按需加载地图」精简主文件（概览/命令/加载表/最小必读事实），细节拆分到专题子文件：
  - 根 → `docs/ai/{deploy,contract,conventions}.md`
  - frontend → `frontend/docs/ai/{deploy-war,architecture,config-env}.md`
  - backend → `backend/docs/ai/{architecture,rest-conventions,security,db-conventions,config}.md`
- 用户约定：前后端同目录管理是过渡形态，后期可能各自独立为根目录；拆分方式要便于将来带走。

## 工作区级 CODEBUDDY.md 已建立
- 根目录新建 `CODEBUDDY.md`（总纲）：记录前后端同目录管理但完全独立、后期可能拆分为独立根目录，只写跨端契约（接口前缀/ApiResponse/两处改接口地址/DB 约定）与 deploy-test.ps1 用法，细节指向 frontend/ 与 backend/ 各自的 CODEBUDDY.md。
- 修正过时描述：frontend/CODEBUDDY.md 中"后端是独立 Fastify 服务"改为 Spring Boot WAR（api.war 同 Tomcat）；backend/CODEBUDDY.md 中前端产物 ROOT.war 改为 hcs.war。

## JWT 安全体系代码评审（未改代码，仅出方案待项目经理决策）
- 评审范围：SecurityConfig / CorsConfig / JwtUtil / JwtAuthenticationFilter / application.properties。
- 主要发现：CORS 全通配 origin=*（P1）；jwt.secret 硬编码且疑似 Base64 字面量未解码（P1）；application.properties 明文含生产 DB 密码（P1）；token 失效/登出无黑名单机制（P2）；filter 重复解析 token 两次（getSubject 再 parse）；401 entryPoint 直接 new ObjectMapper 每次新建；过滤器解析失败无审计日志。方案已列 P0-P2 条目，等待用户指示修改。

## P2 第 5/6/7 条已修复
- JwtAuthenticationFilter：合并 validateToken+getSubject 的双重解析为一次 parseToken（try-catch 区分 ExpiredJwtException / JwtException）；校验失败增加 warn 日志（含 path 与原因，不打 token 明文）。
- SecurityConfig：401 入口点中 ObjectMapper 提取为静态常量复用。
- 仅剩原有风格 warning，无编译错误。
- 用户指示（2026-09-05）：本轮 JWT 评审的多批修改全部完成后才统一执行部署验证，中间批次不做 deploy-test.ps1 打包。
- 用户指示（2026-09-05）：开发阶段代码修改频繁，P1 中 #1（jwt.secret 外置）与 #2（DB 密码外置）暂时忽略，不执行。

## P1 #3 CORS 收紧已实现（改为配置项）
- CorsConfig：origin 硬编码 `*` 改为 `@Value("${cors.allowed-origins:*}")`，逗号分隔多来源。
- application.properties 新增 `cors.allowed-origins=*`（开发/测试阶段取 *；生产 IP 确定后改为具体白名单）。
- lint 无错误。未打包验证（待全部修改完成后统一部署）。

## P2 #8 已完成：删除 jwt.auth-path 死配置
- application.properties 删除 `jwt.auth-path=/auth/**`；白名单路径固定在 `SecurityConfig.PUBLIC_PATHS`（Java 常量，防运行时被改）。
- 同步更新 backend/CODEBUDDY.md 中 jwt.* 配置说明。

## P1 #4 延期
- token 吊销机制记录至 `backend/task/todo.md`，延期执行，待架构师讨论（候选：黑名单 / refresh token / iat 比对）。

## P2 #9 忽略
- 用户明确：JWT 只做身份验证，角色/权限体系尚未明确固定，#9（token 写入角色 claim + 过滤器构建 authorities）不执行。

## P2 #10 已完成：401 业务码细分
- 新增 `common/JwtAuthenticationException`（继承 AuthenticationException，业务码 40101 过期 / 40102 无效）。
- JwtAuthenticationFilter 三条失败路径（缺头/过期/无效）分别携带对应业务码；commence 签名改为接收 AuthenticationException。
- SecurityConfig 入口点：识别 JwtAuthenticationException 时把业务码写入响应体 code（HTTP 状态仍 401），未知异常回退 40102。
- 前端约定：40101 → 跳登录；40102 → 提示无效。lint 仅剩原有风格 warning。
