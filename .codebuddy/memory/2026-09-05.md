# 2026-09-05

## 分页页码规则确认与统一（用户要求）
- 规则确认：前端分页页码 1 基；后端 MyBatis LIMIT 用 0 基偏移，转换在 Controller（`page-1`），已在 sysRight 模块验证全链路。
- MyBatis LIMIT 边界实测（page.sysright 共 23 条）：尾页不足 pageSize 时正常返回剩余行（LIMIT 20,10 返回 3 条），不存在「1 条查不出来」；仅 offset ≥ total（页码超界）返回空。
- JPA：`PageRequest.of` 页码 0 基，Hibernate 内部换算 offset=page*size 并自动 count；忘减 1 会让第一页静默错位到第二页。
- 发现并修复响应不一致：JPA `listPaged` 响应 `page` 原为 0 基（未 +1），MyBatis search 为 1 基。用户确认后统一修改：13 个 Service、14 处 `PageResult.of(..., page, size)` → `page + 1, size`（WorkflowService 2 处含 findBySender，控制器硬编码 0）。`WorkflowService.todo/done` 收 1 基、内部 `(page-1)*pageSize`，自洽未动。全后端响应 `page` 现统一 1 基。
- 待生效：需 `.\deploy-test.ps1 -Part Back` 重新打包部署。

## 分页参数验证与上限规则（用户 2026-09-05 明确，计入 MEMORY.md）
- 模式：前端封装 Query DTO（查询条件 + page/pageSize）POST 到 `/search`；后端先对分页参数验证与限制：page/pageSize 为 null 退默认（1/10），page 最小 1，size 限 1~200（`MAX_PAGE_SIZE`）。
- Controller 层完成 `page-1` 转换，MyBatis/JPA 一律按服务端分页参数计算，Service 不做二次校验。范本：SysUserController.searchByQuery → SysUserService.search。

## 分页注释修正（SysUserController 32-36 行）
- 原注释「退回 DTO 默认值」与实现不符：代码是硬编码 1/10（DTO `SysUserQuery` 默认值恰好也是 1/10，但两者不联动）；原注释还把上限写死成 200，常量改名/改值会失同步。
- 已改为两条注释：①分页参数归一（缺值取默认 1/10、page 下限 1、size 夹在 1~`ConstValue.MAX_PAGE_SIZE`），解释防负 offset 报错与超大结果集；②说明契约 1 基 → Service 0 基偏移，故传 `page - 1`。代码未动。
- IDE 有一条 warning 建议 `Math.min(Math.max(...))` 换 `Math.clamp()`（Java 21+），既有写法，未改。

## 分页归一抽取为 PageParam，全 Controller 铺开（用户要求「不重复注释」）
- 新增 `backend/src/main/java/com/baogang/info/common/PageParam.java`：`record PageParam(int page, int size)`，静态 `of(Integer page, Integer size)` 做归一（默认 1/10、page 下限 1、size 夹 1~`ConstValue.MAX_PAGE_SIZE`），`offset()` 返回 0 基偏移。注释只写在这一处，Controller 侧零注释。
- 已铺开到 16 个 Controller、19 处分页入口：SysUser/SysRole/SysRight/TaskProcess(2)/TransferOrder(2)/TechStep/FlowGraph/ConstValue/BluePrint/Workflow(3)/SysRoleUser/SysRoleRight/FlowNode/FlowHistory/FlowEdge/FlowCurrent。调用形态统一为 `PageParam p = PageParam.of(...); xxxService.search(query, p.offset(), p.size())`。
- 顺带修掉 WorkflowController 两处**既有编译错误**（不是本次引入，文件此前为未提交修改态）：`todo` 调用 `workflowService.todo(query)` 缺 2 个实参（Service 签名 `todo(query,page,pageSize)`，且原局部变量 page/size 算了没用）；`done` 里 `WorkflowQuery query=new WorkflowQuery()` 与形参同名。现均改为 PageParam 归一后传参，并把归一值回写 query（done 原写死 1/30，现接受入参，默认由 30 变 10）。
- 行为变更需留意：①BluePrintController 原 `page<1||size<1` 返回 400，改为静默归一；②前端有多处 `pageSize: 99999` 拉全量（sys/roleUser.vue、sys/roleRright.vue 的 roleAPI/rightAPI.search），现会被截到 200；tech/board.vue 的 200 不受影响。
- 遗留缺陷（未改，待用户决定）：`/workflow/todo`、`/done`、`/sender` 是 `@GetMapping` + `@RequestBody`，前端用 axios `params` 传分页（approval/todo.vue、send.vue、done.vue），GET 无 body → 后端收不到分页参数，翻页实际不生效；sender 更是完全没有分页入参（写死 `findBySender(user,0,30)`，该 Service 用 `PageRequest.of(page,size)` 属 0 基页码，与 offset 数值巧合一致）。
- 未跑 `mvn compile`（命令被跳过），验证方式为逐文件 lint + 全局搜索确认 controller 已无残留裸 `page - 1`。生效需 `.\deploy-test.ps1 -Part Back`。

## 用户偏好补充（2026-09-05）
- 「待处理」中不再列打包部署 / `mvn compile` 编译确认两项，也不主动跑：这些是攒够改动后统一做的收尾动作。正文仍需提示「改动需重新打包部署才生效」。已写入 MEMORY.md 用户偏好。

## 删除 backend/docs/tables.md 并清理引用（用户要求）
- 已删除 `backend/docs/tables.md`（旧 schema 真源：15 表，由 entity `@Column` 提取，2026-08-31）。替代者：`backend/docs/DB/table/`（18 表，一表一文件 + `README.md` 索引，2026-09-05 由库中 `information_schema` 导出）。删除前已当场重读文件头确认内容（遵循 2026-09-03 并发会话事故教训）。
- 引用改写为 `backend/docs/DB/table/`（或 backend 内 `docs/DB/table/`）：`docs/structure.md`（3.5 节表格行）、`docs/ai/conventions.md`、`docs/ai/contract.md`、`backend/CODEBUDDY.md`、`backend/docs/ai/db-conventions.md`、`backend/docs/plans/2026-08-31-drop-flownodeaction.sql`（2 处）。
- `docs/20260903.md` 周报中 3 处属历史事实记录，未删除，改为加注「该文件已于 2026-09-05 删除，真源改为 backend/docs/DB/table/」，避免悬空路径。

## 18 个 Controller 多 Agent 代码评审（只评审未改码）
- 方式：6 个 code-reviewer agent 并行（每组 3 个 Controller + 关联 Service/Repository/Mapper XML/Entity/DTO 间接评审）。因限流分了 4 批跑完。
- 结果：严重 37 / 中等 69 / 建议 75，共 181 条。
- 跨组高频问题（共性模板）：
  1. **认证安全链**（最重）：`AuthController` 明文密码比对、重置密码无凭证（`/auth/**` permitAll 任意账号可被接管）、`jwt.secret`/DB 口令明文在 application.properties、show-sql+bind TRACE 会把密码打进日志。
  2. **FlowEngine 引擎四大缺陷**：越权审批（不校验待办人/fromNode）、`deleteByWorkflow` 误删并行分支、结束节点 `updateState("end")` 被注释（state 永停 start）、无锁并发竞态；另有 3 处 `.get(0)` 无保护。
  3. **Service.update() NPE 模板**：`getById` null 后直接 set → 500，遍布 SysRight/SysRoleRight/SysRoleUser/SysRole/Workflow/FlowGraph/FlowCurrent/FlowHistory 等 Service（应 orElseThrow→404）。
  4. **Controller 编排业务/try-catch 自造响应**：SysRole、SysRight、SysRoleRight/SysRoleUser（批量绑定循环 save 无事务）、BluePrint、TransferOrder、TaskProcess、FlowGraph、TechStep（batchSave 的 `&&` 判空逻辑错误）。
  5. **级联清理缺失**：删图不删节点/边（FlowGraphService）、删节点不删边（FlowNodeService）、删权限/删角色的级联分属两个事务。
  6. **审计字段被破坏**：TransferOrderService.update 覆盖客户端传的 createTime/createUser（严重）；SysUserService.update 无条件覆盖 code；FlowCurrent/FlowHistory update 漏同步部分字段。
  7. **`@Column` 小写 vs 文档驼峰不一致**：FlowNode/FlowEdge/FlowGraph 实体（MySQL 不敏感暂能跑，迁库即断）；X/Y/W/H 例外正确。
  8. **varchar 类型坑确认**：sort 字典序（JPA OrderBySortAsc 与 XML `sort+0` 结果不一致）、dealTime varchar 排序、BluePrintMapper.xml Base_Column_List 漏 3 列、WorkflowMapper.xml 硬编码 `page.` 前缀、rolelist LIKE 子串误匹配。
- 好的方面：XML 全 `#{}` 无注入、PageParam 铺开正确无手写 Math.max/min、todo/done 与 findBySender 页码例外传递正确、start/dealNode 事务完整、SysUser 密码 WRITE_ONLY 防回传。
- 完整报告在当日会话（6 组分节，每条含 文件:行号→修复建议）；未改任何代码，修复排期待用户决定。

## 一致性批修复（评审问题落地，需重打包部署生效）
- **update NPE 模板 ×8**：SysRight/SysRoleRight/SysRoleUser/SysRole/Workflow/FlowGraph/FlowCurrent/FlowHistory Service 的 update 由 `getById(id)` 改为 `repository.findById(id).orElseThrow(ResourceNotFoundException)` → 404。
- **级联下沉单事务**：①`SysRoleService.deleteCascadeByCode(code)`（先删 sysroleright/sysroleuser 绑定，`deleteByCode==0` 抛 404，事务回滚保原子）；②`SysRightService.deleteCascadeByCode(code)`（先 `findByCode` 校验存在→删绑定→删本体）；对应 Controller 只透传，已移除 SysRoleRightService/SysRoleUserService/StringTool 注入与自造 400。
- **图级联清理**：`FlowGraphService.deleteById` 重写为 查图→删边→删节点→删图（修掉 "flowMap not found" 错误消息）；`FlowNodeService.deleteById` 重写为 查节点→删两端边→删节点。新增 Repository 派生方法：FlowEdge `deleteByFlowGraph`/`deleteByFlowGraphAndFromNode`/`deleteByFlowGraphAndToNode`，FlowNode `deleteByFlowGraph`。
- **TransferOrder 审计保护**：update 删掉 createUser/createTime 两行覆盖（新增注释）；"调拨单不存在" 由 IAE(400) 改 `ResourceNotFoundException`(404)。
- **补齐漏同步字段**：FlowCurrentService.update 补 flowGraph；FlowHistoryService.update 补 flowGraph/edge/fromNode/toNode。
- read_lints 全部通过。未跑 mvn compile（按用户偏好攒批统一做）。规范批（Controller try/catch、路由统一、FlowEngine 引擎批）尚未做。

## 实体列名小写检查（用户要求 @Table/@Column 全小写）
- 正则全查 entity 目录（16 个文件）：`@Table`/`@Column` name 全部已是小写，无驼峰列。
- 唯一大写是 FlowNode 的 `X/Y/W/H` 四列；曾按「全小写」改过一次，用户随后指示「X/Y/W/H 忽略」，已还原为原样（大写 @Column + 大写字段名），entity 无净变更、不需部署。
- 结论：小写规则生效范围 = 除 X/Y/W/H 外的所有注解列名。

## 规范批修复（评审问题落地，需重打包部署 + 前端重新 build 生效）
- **try/catch 拆除**：BluePrint/TransferOrder/TaskProcess/FlowGraph 的 update try-catch 删除；@RequestBody 判 null 死代码全删（BluePrint save/update、TransferOrder save/update、TaskProcess save/bind/update、TechStep save/search/update）；TechStep @PathVariable blank 检查删除；TaskProcess 删掉 JsonTool.print(bindMap) 及 import。
- **TechStep.batchSave 修复**：`null&&isEmpty` bug → `isEmpty()` 抛 IAE；新增 `TechStepService.saveBatch`（@Transactional + setId(null) 循环 + saveAll）替换 Controller 逐条 save（原先每条独立事务会部分提交）。
- **404 语义统一**：手写 error(400,"不存在") 全改 ResourceNotFoundException → HTTP 404：BluePrintController(getByCodeAndEdition/delete 0行)、TransferOrderController.getById、TaskProcessController.getById、TechStepController.getById、FlowGraphController.findByWorkflow(原 get(0) 越界 500)。Service：BluePrintService.update/TechStepService.update 的 IAE→RNFE、TransferOrderService.deleteById 补 existsById。success(null) 型 getById 未动（前端判空行为面大，待用户决定）。
- **PUT 路由统一 `/update`**：后端 sysUser/sysRole/sysRight/constValue 由 `PUT /{id}` 改 `PUT /update`（id 取 body，id null 抛 IAE）；constValue 新增 `POST ""`→`POST /save`；flowGraph 删重复 `PUT /{id}` 保留 `/update`。前端 api 同步：user.ts updateUser+update、role.ts、right.ts、process.ts、taskProcess.ts（原调 /{id} 与后端 /update 本就不匹配，顺手修复）、constValue.ts（add→/save）。DELETE /code/{code} 业务键删除保留。
- lint 全通过；FlowGraphController 删掉未用 SysRoleQuery/BluePrint import。

## 引擎批修复（FlowEngine 重写，需重打包部署生效）
- **并发锁**：WorkflowRepository 新增 `@Lock(PESSIMISTIC_WRITE) findByCodeForUpdate`（JPQL），WorkflowService 透传 `getByCodeForUpdate`（404 语义）；dealNode 先锁 workflow 行，并发审批串行化。
- **越权双校验**：① flowGraph 必须等于实例 flowGraph，state=end 拒绝再审批；② edge.fromNode 必须是 flowcurrent 中的当前节点（stream anyMatch，兼容并行分支）；③ 待办人校验 checkAssignee：operator 精确命中 / user∈userList(CSV) / 用户角色(findRolesByuserCode)∩roleList，任一放行，否则 IllegalArgumentException。
- **定向删除**：FlowCurrentRepository 新增 `deleteByWorkflowAndFlowNode`，dealNode 只删被审批节点的当前记录，不再 `deleteByWorkflow` 全删——并行分支保留。
- **end 状态恢复**：目标节点 category=E 时 set state=end + endTime + remark（原被注释）；FlowNode 类别常量 S/E 收敛为常量。
- **get(0) 全保护**：start 的开始节点/出边、dealNode 的边线/起止节点，空集合/不存在均抛 IllegalArgumentException（500→400）。
- **其它**：start 的 code 加 8 位 UUID 后缀缓解同毫秒撞号（原 13 位时间戳并发可撞）；start 返回值由 "start" 改为 workflow.code（前端 grep 无 flowEngine 调用，无影响）；start 前置校验流程图存在；history 的 action 由 "" 改为 "审批"/"start"；FlowEngineController.start 校验 flowType 非空（原 null 会建脏实例）；FlowEngine 注入 SysRoleUserService。
- 行为变化注意：老流程实例的 code 格式变化（WF<ts>_<rand>，30 长度内）；若节点 roleList/userList/operator 全空则无人能审批——现有数据需确认待办人字段已配置。

## SQL 细节批修复（success(null)→404 + Mapper 补列，需重打包部署生效）
- **success(null) 型 getById/getByCode 全改 404**（16 处，10 个 Controller）：SysUser(id+code)、SysRole(id+code)、SysRight(id+code)、ConstValue(id+code)、Workflow(id+code)、FlowGraph(id)、FlowCurrent(id)、FlowHistory(id)、FlowNode(id)、FlowEdge(id)。均在 Controller 层判空抛 ResourceNotFoundException（HTTP 404）。Service 层 getById 保留 orElse(null)（FlowEngine 等内部调用方依赖 null 语义）。
- **前端行为影响**：instance.ts detail(workflow)（审批页详情）查不到的 workflow 现在会抛错弹 message，而不是静默空数据——属预期语义修正。
- **BluePrintMapper.xml 补列**：Base_Column_List + resultMap 补 updateuser/updateTime→updateUser、updatetime/updateTime→updateTime、workflow 三字段（已用 mysql127 查 information_schema 确认 blueprint 表实有 updateUser/updateTime/workflow 驼峰列；MySQL 列名不敏感小写可映射）。消除 MyBatis search 与 JPA findById 读路径不一致。
- **WorkflowMapper.xml** 去掉 `page.` 硬编码 schema 前缀（flowhistory 子查询），换库即断的隐患。
- lint 全通过。未动：rolelist LIKE 子串误匹配（R1 命中 R10，属逻辑改动需确认分隔符格式，待定）、LIKE 前导通配符全表扫描（小表可接受）、varchar 类型列迁移（需 DBA DDL）。

## rolelist LIKE 子串误匹配延期（用户 2026-09-05 指示）
- `WorkflowMapper.xml` Query_Todo 中 `n.rolelist LIKE CONCAT('%', #{q.roleCode}, '%')` 会子串误匹配（R1 命中 R10/R11）。涉及 roleList 存储格式（分隔符约定）的前后端与数据库三方联动，**待与架构师讨论后再改**，本次不动。相关：FlowEngine.checkAssignee 的 inCsv 按逗号分隔解析（前端 roleRright.vue 也是逗号拼接），后端引擎与 Mapper 口径暂不一致，架构师讨论时一并定稿。

## Mapper 全表加 page. 库名前缀（用户 2026-09-05 新指令，推翻 line 83 旧决策）
- **指令**：`backend/src/main/resources/mapper/` 下 9 个 XML 中，每个 `FROM` 后的表名都加 `page.` 前缀，含子查询（flowhistory / flowcurrent a,flownode n）与 workflow 4 处。
- **已落地**：BluePrint/ConstValue/SysRight/SysRole/SysUser/TaskProcess/TechStep/TransferOrder 各 2 处 `FROM`；WorkflowMapper `flowhistory` 子查询、`flowcurrent a,page.flownode n` 逗号连接、4 处 `FROM workflow`。全局校验 `(?i)\bfrom\s+(?!page\.)\w+` 命中 0。
- **与 line 83 冲突的澄清（用户 2026-09-05 确认）**：line 83 当时为规避「换库即断」主动去掉了前缀。现用户明确反向、且给出理由：JDBC URL 是部署配置，被人误改/漏填的概率高于 Mapper XML；放 URL 一旦库名变动所有不带前缀的 SQL 全会找不到表。故**既定设计 = 表名前缀 `page.` 一律硬编码在 Mapper XML**，不放 JDBC URL。库名若真变更需同步改全部 XML（换库即断风险已知并接受）。
- 生效需 `.\deploy-test.ps1 -Part Back` 重打包部署。

## 第二轮 18 Controller 多 Agent 全量评审 + RESTful 审查（只读未改码，2026-09-05 下午）
- 方式：7 个 code-reviewer agent 并行（6 域 × 3 Controller + 1 个全局 RESTful 审查 124 端点），依赖链追到 Service/Repository/Mapper XML/Entity/DTO/SecurityConfig，并用 information_schema 实测库列名/索引。
- **P0 ×5**：①`/auth/resetPassword` 匿名可重置任意账号密码（verify 凭证未传递，账号接管）；②密码全链路明文（需 BCrypt）；③`jwt.secret` 可预测明文 + 生产 DB 密码注释残留；④全局无角色鉴权（SecurityConfig 仅 authenticated）→ PUT /taskprocess/update 可伪造审核通过、PUT /workflow/update 可直接置 state=end 绕过引擎、可自我提权绑权限；⑤FlowNode/FlowEdge Service.update 按(flowGraph,code)查不到时静默返回未持久化实体（假成功，改 code 必触发）。
- **P1 高频**：update 改 code 无查重（SysRight/ConstValue/SysRole/FlowNode save/update/TransferOrder）；SysRoleRight/SysRoleUser save 循环逐条独立事务+竞态；checkAssignee 把 operator 模式码当用户名 equals；并行汇合无令牌计数；workflow.code 无索引使 FOR UPDATE 全表锁；待办三口径分裂（LIKE 子串/operator='R' 漏 M/P/不过滤 state=end）；BluePrintQuery.keyword 死字段；FlowGraphService.update 漏 title；TechStep `@Valid List` 不级联；@Size 与库列宽大面积失配（techstep/taskprocess/blueprint.weight）；GlobalExceptionHandler 兜底 500 不打日志。
- **P2**：实体 @Column/XML 列名小写 vs 库中驼峰（全项目性，与「注解全小写」规则冲突需统一口径）；Service 形参 page/pageOffset 实为 0 基误导；约 15 处死代码；@Size 消息复制粘贴错；axis 100 存 JSON 易超。
- **RESTful 结论（124 端点）**：RPC over POST 实用主义；底线守住（57 个 GET 全只读、无 GET 写操作、写方法选型正确、SQL 全参数化）；PUT /{id} 与 /update 两种写法曾并存（已统一 /update，FlowEdgeService.update 仍 body id）；建议内部系统不必全盘 REST 化，但错误语义要统一（Auth 手写 body code vs 全局 400/404 两套并存）。
- 完整分组报告（每条 文件:行号+修法）在当日会话记录。修复排期：立即=resetPassword 凭证链+BCrypt+密钥外置；短期=角色矩阵+update 改按 id+唯一索引；迭代=待办口径（roleList 分隔符待架构师定稿）+列宽对齐+异常日志。

## 规则定稿：删除「以库中实际拼写为准」，DB 列名小写化交 DBA（2026-09-05）
- 用户决策：「注解列名全小写」与「以库中实际拼写为准」冲突，**删除后者**。新规则 = 以代码为准：`@Column`/XML 列名全小写，唯一例外 `flownode.X/Y/W/H` 保持大写。
- 实测 `page` 库含大写字母的列共 72 个（15 张表），扣除 4 个例外后 **68 个列需改名**。已生成 DDL 文本 `backend/docs/plans/2026-09-05-lowercase-columns.sql`（RENAME COLUMN 语法，MySQL 8.0+；含执行后验证 SQL——除 X/Y/W/H 外不应再有大写列）交 DBA 执行，AI 未连库执行。
- 执行时机安全：代码全用小写列名 + MySQL 列名大小写不敏感，改名前后应用均正常，无需停机窗口；RENAME COLUMN 不重建表、不丢数据。
- 拼写遗留只改大小写不改拼写：`approval.startTime→starttime`（疑 startTime 笔误）、`flowgraph.height`（本已小写）。拼写是否纠正（startTime→startTime、height→height）待用户另定，需另出 DDL。
- 文档同步：`backend/docs/DB/table/README.md` 尾部通用说明块重写（新规则 + DDL 路径）、`approval.md`/`flowgraph.md` 的拼写遗留注解更新；MEMORY.md「列名/表名大小写例外」条目整体重写为「列名大小写规则」，「表文档列名规范」条目补充 DDL 执行前后状态说明。

## PUT 语义最终统一（用户指令：全部用 PUT /update，id 在 body）
- 剩余 4 个 `PUT /{id}` 全部改为 `PUT /update`（id 取 body、null 抛 IAE "修改操作必须传入 id"，Service 签名 update(id, entity) 未动，Controller 显式传 entity.getId()）：FlowCurrentController、FlowHistoryController、SysRoleRightController、SysRoleUserController。
- 顺带补齐 WorkflowController.update 缺失的 id 判空（原 id null 时 findById(null) 抛 InvalidDataAccessApiUsageException → 500）。
- 前端核查：`frontend/src/api` 对这 4 个资源无任何 PUT 调用（roleUser/roleRight/instance 只用 get/post/delete），前端零改动。
- 全局校验：controller 目录 `@PutMapping("/{id}")` 命中 0；至此 16 个有 PUT 的 Controller 全部统一为 `PUT /update`（id 在 body）。lint 仅剩 save 方法既有的 unchecked cast warning（已知 P2，非本次引入）。
- 生效需 `.\deploy-test.ps1 -Part Back`。此前评审提到的 FlowEdgeService.update 用 (flowGraph,code) 定位、FlowNode 同病的 update 语义重构（按 id + 404）是另一独立问题，本次未动。

## URL 风格定稿（用户 2026-09-05 决策）
- 用户确认：**动词式 URL，不严格 RESTful——URL 要反馈操作方法信息**。RESTful 审查报告中「16 个 /save、12 个 /update、9 个 /search 等动词路径」不做 REST 化改造，维持现状即规范。
- 约定形态：`POST /xxx/search`（复杂条件）/ `GET /xxx/list` / `POST /xxx/save` / `PUT /xxx/update`（id 在 body，null→400）/ `DELETE /xxx/{id}`（业务键可用 `/xxx/code/{code}`）。新接口照此写，勿改裸资源或 `PUT /{id}`。
- 已写入 `docs/ai/contract.md` 第 2 条（原第 2 条扩充定稿表述）与 MEMORY.md 项目约定。代码零改动。

## 错误语义现状核实与定稿（用户告知：已删除 404 的 code；2026-09-05）
- 实测确认：Controller 层手写 `error(404, ...)` 已清零——业务「不存在」全部走 `ResourceNotFoundException` → 全局处理器 HTTP 404 + body code 404（唯一 404 出口）。
- 手写 `ApiResponse.error(400, ...)` 仅剩 22 处：AuthController 20 处（login/verify/resetPassword/register 的业务校验）+ SysRoleUser/SysRoleRight save 各 1 处「参数错误」，均为 HTTP 200 + body code 400。
- 前端 `http.ts` 响应拦截器双通道兼容：①HTTP 2xx 时判 `res.code !== 200` reject；②HTTP 非 2xx 时解包 body `{code, msg}` 取 message。两种错误形态调用方统一拿 `err.message`。
- 结论：评审报告中「错误语义两套并存」项关闭——404 已单轨化，剩余 HTTP 200 + body 400 形态属既定可接受（配合动词式 URL 的实用主义），前端无需感知差异。
- 已写入 `docs/ai/contract.md` 第 2 条「错误语义现状」。代码零改动。

## 过滤值路径段形态定稿（用户 2026-09-05 决策）
- 用户确认：`/state/{state}`、`/category/{category}`、`/workflow/{workflow}`、`/flowGraph/{flowGraph}`、`/code/{code}` 等过滤值放路径段的端点**保留不修改**，不改成 query 参数。
- RESTful 审查报告中「过滤值放路径段建议改 query」一条就此关闭。至此 RESTful 审查全部风格类建议（动词 URL、PUT 统一 /update、过滤值路径段、错误语义）均已定稿关闭，只剩安全/功能类遗留待安排。
- 已写入 `docs/ai/contract.md` 第 2 条。代码零改动。

## P0 记录到 backend/task/todo.md（用户指令：先记录待架构师评审，不改代码）
- 已把第二轮评审的 5 个 P0 追加到 `backend/task/todo.md`（新增章节「[待架构师评审] P0 安全与正确性五项」）：①resetPassword 匿名重置（账号接管）；②密码明文（BCrypt）；③JWT 密钥可预测+凭据残留；④全局无角色鉴权（三条旁路审批路径）；⑤FlowNode/FlowEdge update 静默假成功。每条含位置/问题/候选方向，附 P1 关联背景与 DBA 待办引用。代码零改动，等待架构师评审定方案。

## 列名小写化 DDL 执行完毕（用户告知 DBA 已执行，2026-09-05）
- AI 用 SQL 文件尾部的验证 SQL 实测（information_schema + utf8mb4_bin 正则）：`page` 库除 `flownode.X/Y/W/H` 四个例外列外**0 行大写列残留**，68 个改名全部生效。
- 至此代码（`@Column`/XML 小写）↔ 表文档（`backend/docs/DB/table/*.md` 小写）↔ 库实列三者完全一致，MySQL 大小写不敏感的隐性依赖解除。
- 状态同步：MEMORY.md「列名大小写规则」「表文档列名规范」两条改为已执行；`backend/docs/DB/table/README.md` 尾部说明改为已执行并验证；`backend/task/todo.md` 中该待办标记完成。
- 拼写遗留不变：`approval.starttime`、`flowgraph.height` 仅小写化，拼写纠正（starttime→starttime、height→height）仍待用户另定。

## 拼写纠正完成（sarttime→starttime、heght→height，用户告知已完成，2026-09-05）
- AI 实测 information_schema 确认：`page.approval.starttime`（原 sartTime）、`page.flowgraph.height`（原 heght）均已改名，旧拼写无残留。
- 代码影响核查：`FlowGraph` 实体本就映射 `@Column(name = "height")`（改名后反而精确对齐）；`approval` 表在 backend/src 中无任何引用（无实体/无 Controller/无 Mapper XML）；前端 src 无 sart/heght 引用——**代码与前端零改动、JSON 契约不变**。
- 文档同步：`backend/docs/DB/table/README.md` 拼写说明、`approval.md`/`flowgraph.md` 的过时「未纠正」说明改为已纠正（用户已先行把两文档数据行列名改为 starttime/height）；MEMORY.md 拼写遗留条目关闭。
- 至此列名治理全部完成：68 列小写化 + 2 处拼写纠正，库/代码/文档三者一致。

## GlobalExceptionHandler 补日志 + 客户端错误区分映射（用户指令落地，需重打包部署）
- 修改 `exception/GlobalExceptionHandler.java`：
  - 兜底 500：补 `log.error("未处理异常 uri={} method={}", uri, method, ex)`（注入 HttpServletRequest 取 uri/method）；
  - 新增 4 个映射：`MethodArgumentTypeMismatchException`→400「参数类型错误: {name}」；`HttpMessageNotReadableException`→400「请求体格式错误或缺失」（落 warn 日志）；`NoResourceFoundException`→404「接口不存在: {resourcePath}」（未知 URL 不再 500）；`DataIntegrityViolationException`→409「数据冲突：业务键可能已存在」（落 warn 日志，为后续补唯一索引后竞态落地做准备）；
  - 日志走 slf4j（根级 INFO，error/warn 均会输出到 console/catalina 日志）。
- lint 通过。行为变化：①未知 URL 由 500→404；②非法 JSON/路径参数类型错误由 500→400；③唯一键冲突由 500→409；④兜底异常可见于日志。
- todo.md 中该条已标记已修复；`@Size` 列宽失配仍待处理。生效需 `.\deploy-test.ps1 -Part Back`。

## 业务数据域 6 项 P1 修复（用户指令落地，需重打包部署生效）
1. **BluePrintQuery.keyword 实现**：BluePrintMapper.xml Query_Where 头部新增 keyword 条件（code/name/materialname/materialcode/customer/model/remark 七字段 OR 模糊），count 与 query 共用该片段自动生效；顺带删除 Query 中无列无条件的死字段 `category`（含 getter/setter，全库无引用）。前端当前未传 keyword，行为修正后可随时接入。
2. **FlowGraphService.update 补 setTitle**：修复改标题静默丢失。
3. **TechStepController.batchSave**：`List<TechStep>` → `List<@Valid TechStep>`，元素级联过 @Size 校验（校验失败由全局 MethodArgumentNotValid → 400）。
4. **@Size/@Column length 按库列宽对齐**（真源 = backend/docs/DB/table/*.md）：TechStep 全部改为 45（remark 100）；TaskProcess 审计/状态类改 20（audituser/audittime/auditmessage/auditstate/state/createuser/createtime/updateuser/updatetime），step 保持 100；BluePrint name 50→100、weight 45→100、createuser/createtime/updateuser/updatetime/workflow 100→45。消除「合法数据被 400 误杀 / 脏数据入库报 500」双向失配。
5. **TaskProcessService.deleteById 补 existsById → ResourceNotFoundException(404)**，与 TransferOrderService 对齐（原不存在 id 抛 EmptyResultDataAccessException → 500）。
6. **BluePrint save (code,edition) 防重**：BluePrintRepository 新增 `existsByCodeAndEdition`，save 冲突抛 IllegalArgumentException(→400「蓝本已存在：code=…, edition=…」)，消除重复插入后 findByCodeAndEdition（Optional 单行语义）永久 500 的隐患。根治仍需 DBA 建 (code,edition) 唯一索引（已在 todo.md P0 背景记录）。
- lint 全部通过；category 死字段无残留引用；前端零改动（未使用 keyword）。生效需 `.\deploy-test.ps1 -Part Back`。

## 工作总结类 Skill 选型（供项目经理选型，仅调研未安装）
- 查找路径：`find-skills` 技能 → 本地已安装扫描（本地无工作总结类）+ SkillHub（lightmake.site /v1/search）+ 虾评（xiaping.coze.com /api/skills/search）。
- 结论：主推 SkillHub「工作总结」`work-summary1`（618 下载，覆盖周/月/季/年度，主动补齐工作内容·受众·汇报对象，量化数据、分层汇报）；备选虾评「工作日报生成器」id `79750b4a-5121-4225-ac65-70f3912a2aa8`（v1.6.0，日报粒度，三种模板，无需 API Key）。
- 虾评侧同类多为体制内公文写作向（盐边写材料助手、公笔智库、党务助手），与本研发团队场景不匹配，已排除。

## Mapper 四源对齐审计 + XML 小写强制（3 agent 并行，2026-09-05）
- **任务**：9 个 Mapper XML 对照 4 源（XML / `com/baogang/info/dto` / `com/baogang/info/mapper` 接口 / `backend/docs/DB/table` 表文档）查 参数与属性对齐；并要求 XML 列名/表名全小写。3 个 code-reviewer agent 各管 3 个 mapper 并行。
- **结论：9 个 XML 全部通过，0 处需改**。①XML `id` 与 Mapper 接口方法一一对应；②`q.*` 引用全部命中对应 Query DTO 字段；③SELECT/resultMap/WHERE 列全部命中表文档列（大小写不敏感）；④表名与列标识符已全小写、`page.` 前缀已就位（含 WorkflowMapper 子查询 flowhistory/flowcurrent/flownode）。
- **文档列名大小写（用户 2026-09-05 决策：已执行）**：`docs/DB/table/*.md` 原 COLUMN_NAME 用驼峰（`firstLevel`/`materialCode`/`auditUser`/`startTime`/`flowGraph`…），而 XML / entity `@Column` / README(line 24) 规则均为小写。用户拍板：**把 18 个表文档列名统一规范为小写**（对齐 README 规则与代码实际写法），唯一例外是 `flownode` 表的 `X/Y/W/H` 四个大写列。已用 Node 脚本批量处理（只改数据行第 2 单元格、保留表头/分隔行与 X/Y/W/H），共 182 个列名单元格转小写。大小写敏感校验确认全库文档数据行仅剩 flownode 的 X/Y/W/H 仍大写。性质说明：`page` 库实列本以驼峰为主，文档现按约定呈现小写，靠 MySQL 列名大小写不敏感与代码一致；此改动使文档与 README/XML/entity 完全统一。
