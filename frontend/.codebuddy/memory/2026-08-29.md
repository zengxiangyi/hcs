# 2026-08-29

## workflow/draw.vue
- 连线表格「开始节点 / 目标节点」两列由只读文本改为 `el-select`（v-model 直连 `row.fromNode` / `row.toNode`），选项来自新增的 `nodeOptions` computed（节点 code 为 value，显示为 `名称（编号）`），列宽 100 → 160。
- 删除不再使用的 `nodeLabel()`（项目开启 `noUnusedLocals`，未使用函数会导致 vue-tsc 报错）。
- 顺带修复既有类型错误：`createNode()` 里的 `axis: '0,0'` 不属于 `FlowNode`，改为初始化 `X/Y/W/H` 为空串（与弹窗表单字段一致）。

### 修正（同日回滚）
- 按用户要求将上述两列改回只读文本：`el-select` 上的 `readonly` 属性无效，改为 `{{ nodeLabel(row.fromNode) }}` / `{{ nodeLabel(row.toNode) }}`，与「分类」列一致走只读展示；连线编辑唯一入口仍是弹窗。
- 恢复 `nodeLabel(value)`：优先按节点 `id` 匹配、其次按 `code` 匹配（兼容历史数据），展示 `名称（编号）`，空值显示 `-`。
- 删除因此不再被读取的 `nodeOptions` computed（`noUnusedLocals` 会告警）。

### 清理无效代码（同日第二轮）
- 删除 `extractSeq()` 及 `load()` 中的「用后端 id 反解自增基数」校准逻辑：前端生成的 id 格式为 `N<ts>_<seq>`，后端返回的 id 不匹配该正则，校准恒为 no-op。
- 合并 `watch(flowGraph)` + `onMounted(load)` 为 `watch(flowGraph, load, { immediate: true })`，清空列表逻辑移入 `load()`；移除 `onMounted` 导入。
- 修复表格 Y 列 label 误写为「X坐标」；删除仅含空白的行与重复的 `<!-- 流程图显示 -->` 注释。
- 移除 X/Y/W/H 四个 `el-form-item` 上无对应校验规则的 `prop` 属性。
- 「操作人分类」编辑由 `el-input` 改为 `el-select`（数据源 `operatorOptions`），并删除该选项中 `{ value: '', label: '-' }` 这条空值项（空值展示已由 `optionLabel` 兜底为 `-`，改 clearable 即可清空）。
- `createNode()` 默认 `category` 由 `'task'` 改为 `'T'`、`createEdge()` 由 `'normal'` 改为 `'A'`，与页面选项表 `categoryOptions`(S/T/D/E)、`edgeCategoryOptions`(A/B/C) 对齐；注意 `src/api/flow.ts` 的类型注释仍写的是 start/task/decision/end、normal/condition/default，需与后端确认真实取值。
- `removeNode()` 级联清理连线时按 `n.id` 过滤，但连线下拉与 `nodeLabel()` 均以节点 `code` 为键，改为按 `n.code` 过滤。

## workflow/draw.vue renderCanvas 重写
- 将示例硬编码的 nodes/edges 改为依据 `nodes.value` / `edges.value`（`FlowNode`/`FlowEdge`）绘制：
  - 节点坐标取自 `X`/`Y`（字符串，`parsePoint` 解析，非法用默认 50），尺寸取自 `W`/`H`（`parseNum`，默认 120x60），颜色 `color`，形状 `shape`（rect/round/diamond/circle，`traceNodePath` 分别绘制）。
  - 连线 `fromNode`/`toNode` 以节点 `code` 互连，颜色 `color`，可选折线 `axis`（支持 `x,y;x,y` 或 `x,y,x,y` 两种格式，`parseAxis`），条件 `cond` 作为连线中点标签。
  - 画布尺寸依据节点坐标+边距自动计算，`setTransform(ratio,...)` 替代旧 `scale()`，先绘连线后绘节点。
- 新增 `drawArrow()` 统一绘制箭头；删除了 `renderEdge` 里的 `debugger`。
- 注意 `src/api/flow.ts` 类型注释（category: start/task/decision/end、axis 为字符串）与页面实际取值需与后端确认。

## workflow/draw.vue parseAxis 支持对象数组
- 用户要求 `parseAxis` 支持 `edge.axis` 为坐标点对象数组（`[{x,y},...]`）。
- 将 `parseAxis` 入参类型改为 `type Axis = string | AxisPoint[] | undefined`（`AxisPoint = {x,y}`），新增数组分支：`Array.isArray(axis)` 时直接 `filter` 有效点（`x`/`y` 均 `Number.isFinite`）；保留原有字符串分支（`x,y;x,y` 与 `x,y,x,y`）作兼容。
- 同步将 `src/api/flow.ts` 的 `FlowEdge.axis` 类型从 `string` 改为 `Array<{x,y}>`；`draw.vue` 的 `createEdge()` 默认 `axis` 由 `''` 改为 `[]`。
- 后续用户明确「不通过分隔符区分」：`parseAxis` 简化为仅接受坐标点对象数组（`AxisPoint[] | undefined`），`Array.isArray` 判断后 `filter` 有效点，删除原字符串分号/逗号解析分支；不再有分隔符格式兼容。
