# 2026-08-27

## tech/draft.vue 蓝本编辑信息查看页
- 需求：`draft.vue` 通过 code 加载蓝本编辑信息，`onMounted` 初始化数据，router 跳转传 code。
- 改动：
  - `src/api/tech.ts`：新增 `techAPI.getByCode(code)`，请求 `GET /api/blueprint/code?code=`。
  - `src/components/tech/draft.vue`：引入 `onMounted` + `useRoute`，新增 `loadByCode`，在 `onMounted` 中读取 `route.query.code` 回填 basicForm/requirementForm。
  - `src/router/index.ts`：`tech/draft` 路由名由 `Board`（与 board.vue 冲突）改为 `Draft`。
  - `src/components/bluePrint/list.vue`：`handleEdit` 改为 `router.push({ name: 'Draft', query: { code: row.code } })`，并引入 `useRouter`。
- 注意：后端需新增 `GET /api/blueprint/code?code=` 接口（当前 tech.ts 仅 `get(id)`）。draft.vue 表单保持可编辑（查看后可继续编辑/另存）。
