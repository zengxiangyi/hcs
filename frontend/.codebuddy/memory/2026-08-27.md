# 2026-08-27

## 公共样式抽取（重要）
- 新建全局（非 scoped）`src/style/common.css`，由 `main.ts` 引入：`.page-title`（品牌竖条）、`.query-form`（白底卡片查询区）、`.toolbar`、`.el-table`（卡片圆角 + 表头渐变 + hover 高亮）、`.el-table .el-button.is-link`（操作按钮 hover 动效）、`.pagination`。
- `sys/constValue.vue` 清空 scoped 样式，只留 `.sys-page` 私有样式，模板/JS 零改动。
- 用户选「立即批量迁移」，已删除下列页面 scoped 中与公共样式重复的规则（仅保留各自容器类与私有样式）：sys 的 user/role/right/roleUser/roleRright、approval 的 todo/send/done、process 的 task/process/process_h、data/data1/data2、info/file、product/transfer、bluePrint/list。
- 无需改动（使用私有类）：tech/board、tech/draft、tech/bak1、tech/plan/TZ01、approval/graph。
- 关键点：各页 scoped 因带 `[data-v]` 属性选择器特异性更高会覆盖全局，故抽离公共样式不会破坏现有页面。模板/JS 全程未改，lint 通过。

## 字体与配色
- `src/style.css` 的 `:root` 新增 `--color-text-main: #000000`、`--color-text-aux: #000000`；`board.vue` 全部写死字色改为 var() 复用。
- `common.css` 的 `.el-table` 加 `--el-table-text-color:#000000`、`--el-table-header-text-color` 由 #606266 改 #000000，并加 `color:#000` 兜底 → 所有表格文字统一黑色。

## 纯 CSS 美化（不改 JS/template）
- `tech/board.vue` 修底部拉长：根因 `.bottom-btn` 与 `.tech-board` 同在 `.layout-right`，父级 `.layout-right > *` 的 `min-height:100%` 强制拉长（块级布局下 `flex:0 0 auto` 无效）→ 加 `min-height:auto;height:auto` 覆盖 + `position:sticky;bottom:0`；`.tech-board` 移除 flex/overflow-y/max-height，滚动交给父级。区块卡片化、标签加粗 + `::after` 冒号、输入框圆角 + 聚焦蓝色光晕。
- `Web.vue`：顶栏深蓝渐变（#1e3c72→#1e5aa8→#1e90ff）+ 底部高光细线 + 阴影，高 50→56px（`.layout` 的 `calc(100vh - 56px)` 需同步）；左栏纯白 + 右侧分隔线 + 阴影 + overflow-y:auto，min-width 180→200px；内容区背景 #f5f6fa + padding 16 + 卡片化。
- `MenuBar.vue`：一级菜单圆角卡片（margin 2px 8px + padding 10px 12px + radius 6px），hover #eef3fb，active 左侧 3px 竖条 + 渐变背景 + 深蓝字；子级字号 13px、缩进 34px、引导竖线 26px。
- `Login.vue`：背景改深蓝渐变 + 两处 blur(90px) 光晕装饰；卡片 360→400px、双层阴影；新增纯装饰品牌区（HB 圆形 logo + 「用户管理系统」）；标题左对齐 22px + 底部渐变短线；输入框 44px、按钮 46px + letter-spacing。
- `sys/constValue.vue`：查询/工具/表格/分页四区卡片化，表头渐变，分页 active 渐变蓝。

## 功能改动
- `tech/draft.vue` 蓝本编辑查看页：`src/api/tech.ts` 新增 `getByCode(code)`（`GET /api/blueprint/code?code=`）；draft.vue 用 `onMounted` 读 `route.query.code` 回填 basicForm/requirementForm；路由名由 `Board`（与 board.vue 冲突）改 `Draft`；`bluePrint/list.vue` 的 handleEdit 改 `router.push({name:'Draft', query:{code: row.code}})`。**后端需新增该接口。**
- 登录权限持久化：`src/api/auth.ts` 的 `LoginResult` 增 `rights: string[]`；`Login.vue` 登录成功后 `setCurrentUser(data.rights)`；`Web.vue` 登出时 `removeItem('rights')` + `restoreCurrentUserRights()`。**后端 /api/auth/login 需下发 rights（用户另行处理）。**
