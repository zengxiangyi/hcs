# 2026-08-25

## tech 工艺看板 board.vue 编制方案改造

- 确认采用「方案一」：每种工艺编制独立成组件，按二级工艺动态切换（`<component :is>`），放弃 div 显示/隐藏方案。原因：9 种方案差异大（表单+表格），组件化隔离状态、易扩展。
- 新增目录 `src/components/tech/plan/`：
  - `types.ts`：`PlanModel { fields: Record<string, any>; rows: Record<string, any>[] }` + `createEmptyPlan()`。
  - `SandPlan.vue`：示例组件（油冷调质，含工艺参数表单 + 编制明细 el-table），通过 `defineModel<PlanModel>()` 双向绑定。
- `board.vue` 改造：
  - 引入 `planComponentMap`（`secondCraft value → Component`）与 `currentPlan` 计算属性、`planModel` (shallowRef)。
  - `watch(firstCraft)` / `watch(secondCraft)` 切换时重置 `planModel`，避免脏数据。
  - 工艺编制区块改为 `<component :is="currentPlan" v-model="planModel" v-if="currentPlan"/>` + 未选时 `el-empty`。
  - `onSave` 合并 `firstCraft/secondCraft/basic/requirement/template/plan`；`onCancel` 重置 `planModel`。
  - 删除了原 `tempRows` 的死函数 `addTempRow`/`removeTempRow`（原本模板未引用）。
- Lint 验证：tech 相关 3 文件 0 错误。注意 `npm run build` 中 Login.vue/transfer.vue/right.vue/user.vue/bak1.vue 仍有预先存在的 TS 错误，非本次引入，未处理。

## 待办（后续）
- 其余 8 种方案组件按 `SandPlan.vue` 模板新建，并在 `planComponentMap` 注册（键为 craftTree 中二级工艺的 value）。
