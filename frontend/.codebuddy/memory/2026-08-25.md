# 2026-08-25

## tech/board.vue 工艺编制方案改造
- 选型：每种工艺编制独立成组件，按二级工艺用 `<component :is>` 动态切换（放弃 div 显隐方案），因 9 种方案差异大（表单 + 表格），组件化可隔离状态、易扩展。
- 新增 `src/components/tech/plan/`：
  - `types.ts`：`PlanModel { fields: Record<string,any>; rows: Record<string,any>[] }` + `createEmptyPlan()`。
  - `SandPlan.vue`：示例组件（油冷调质，工艺参数表单 + 编制明细表），用 `defineModel<PlanModel>()` 双向绑定。
- `board.vue`：引入 `planComponentMap`（二级工艺 value → 组件）、`currentPlan`、`planModel`；两级 watch 切换时重置 planModel 防脏数据；编制区块改 `<component :is="currentPlan" v-model>` + 未选时 `el-empty`；`onSave` 合并 firstCraft/secondCraft/basic/requirement/template/plan，`onCancel` 重置。删除死函数 addTempRow/removeTempRow。
- 已知既有 TS 错误（非本次引入，未处理）：Login.vue、transfer.vue、right.vue、user.vue、bak1.vue。

## 待办
- 其余 8 种方案组件按 TZ01 模板新建，并在 `planComponentMap` 注册（键为 craftTree 二级工艺 value）。
