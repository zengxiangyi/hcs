# 2026-08-28

## roleRright.vue 纯 CSS 风格重设计
- 仅修改 `<style scoped>`，未改动 template 和 script。
- 页面整体：渐变背景 + 白色卡片化左右面板，圆角阴影。
- 角色选中高亮：`.el-menu-item.is-active` 渐变蓝底白字 + 左侧指示条。（已回退）
- 权限选中高亮：权限项改为胶囊按钮风格，选中态渐变蓝底白字（`el-checkbox.is-checked .el-checkbox__label`）。
- 使用 `:deep()` 穿透 Element Plus 内部类，scoped 样式生效。

## 更新：角色菜单样式回退（2026-08-28）
- 用户要求回退角色样式，`.role-menu` 恢复为原始仅 `border-right:none; max-height:420px; overflow:auto`，移除自定义选中高亮（渐变底、指示条、滚动条美化）。

## 更新：权限样式回退（2026-08-28）
- 用户要求回退权限样式，权限分组卡片、分组头、权限项（胶囊按钮风格）、底部保存栏均恢复为原始默认样式。
- 当前最终态：页面整体美化（渐变背景、卡片面板、 标题指示条、提示条）保留，角色菜单与权限项均为原始 el 默认样式。

## roleUser.vue 用户列表增加分页（2026-08-28）
- 改用服务端分页：`sysUserAPI.search({page, pageSize})`，新增 `currentPage/pageSize/total` 状态与 `el-pagination`（含 sizes/跳转）。
- 切换角色时 `currentPage` 重置为 1；翻页/改页大小触发 `handlePageChange` 重新拉取。
- 勾选状态 `checkedUserCodes` 按 code 存储，跨页保持一致。
