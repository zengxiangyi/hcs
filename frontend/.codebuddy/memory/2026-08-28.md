# 2026-08-28

## sys/roleRright.vue 纯 CSS 重设计（后回退）
- 仅改 `<style scoped>`：渐变背景 + 白色卡片化左右面板；角色选中渐变高亮 + 左侧指示条；权限项改胶囊按钮（`:deep()` 穿透 Element Plus 内部类）。
- 回退（用户要求）：角色菜单恢复为仅 `border-right:none; max-height:420px; overflow:auto`；权限分组卡片、分组头、权限项、底部保存栏全部恢复 el 默认样式。
- **最终态**：只保留页面整体美化（渐变背景、卡片面板、标题指示条、提示条）。

## sys/roleUser.vue 服务端分页
- 改 `sysUserAPI.search({page,pageSize})`，新增 `currentPage/pageSize/total` + `el-pagination`（含 sizes、跳转）。
- 切换角色时 currentPage 重置为 1；翻页/改页大小走 `handlePageChange`。勾选 `checkedUserCodes` 按 code 存储，跨页保持一致。

## product/transfer.vue 去 mock + 新增对话框
- 调拨单 → `transferAPI.search`（`/api/transfer/search`，服务端分页）；蓝本 → 真实存在的 `blueprintAPI.search`（`/api/blueprint/search`），字段映射 code→blueprintNo、name→blueprintName、model→spec、specs→material、edition→version；绑定 → `transferAPI.bindBlueprint`（改 async + try/catch）。
- 模板「调拨日期」列 prop 由 `transferDate` 修正为 `datetime`。
- 「新增」复用 toolbar 的 handleAdd 弹 `el-dialog`：表单每行 3 列（`:span=8`, gutter 16），含创建人(createUser)/创建日期(createTime) 与状态下拉；急件说明占 16 栅格；保存校验后走 `transferAPI.create`（`/api/transfer/create`）。
- `src/api/transfer.ts` 重写为真正的调拨单接口（TransferRow / TransferQueryParams / TransferListResult / TransferCreateParams / BindBlueprintParams）；`BindBlueprintParams` 参数修正为 transferId/blueprintId 以匹配实际调用。
- **后端(../backend)当前无 transfer 相关接口，路径为约定，待实现后生效；蓝本接口已真实存在。**

## process/task.vue 重做
- 新建 `src/api/taskProcess.ts`：`TaskRow`(id/transfer/blueprint/auditState/step/state/createUser/createTime) + `taskProcessAPI`(search/add/update/remove) → `/api/taskProcess/*`。
- `task.vue` 删除全部本地 mock，改服务端分页；列：序号、ID、调拨单、蓝本工艺编号、审批状态(tag)、工序、状态(tag)、创建人、创建时间、操作(删除)；查询覆盖 transfer/blueprint/step/auditState/state；删除走 `ElMessageBox.confirm` + remove。
- **约定接口，后端待实现。**

## 流程图 graph
- 新建 `src/api/graph.ts`：`GraphRow`(id/flowGraph/title/width/heght/remark) + `graphAPI.search`(POST 分页)/save/edit/remove(`DELETE /api/graph/:id`)。
- 新建 `src/components/graph/graph.vue`：表格列（流程图编号/标题/宽度/高度/备注）+ 查询栏 + 新增/编辑弹窗（el-form 校验）+ 删除确认 + 服务端分页。
- **字段名 `heght` 按用户原始拼写保留（疑似 height 笔误）；后端 `/api/graph/*` 待实现。**

## workflow/draw.vue 流程图设计页
- 原为空文件，导致「流程图设计」菜单/flow.vue 跳转后空白页。已实现三部分布局：
  - 上部节点定义表：id/flowGraph/code/name/category(下拉)/shape(下拉)/axis(x,y)/color(取色器)/operator/roleList/userList；
  - 下部连线定义表：id/flowGraph/code/name/category/fromNode(节点下拉)/toNode/cond/remark/color/axis；
  - 底部 canvas：按 axis 定位节点，按 fromNode→toNode 自动连线并绘箭头，cond 作连线标签。
- 用户要求「两表上下排列」，遂移除原 `el-tabs`（节点/连线标签页），改为上下两个 `.flow-section` 卡片，删除 `activeTab`，区块头含标题 + 数量 + 新增按钮。
- 读路由 query `flowGraph`（`useRoute().query.flowGraph`），由 workflow/flow.vue 的 openShow 经 `router.push({name:'draw', query:{flowGraph}})` 传入；mounted 及参数变化时重新 load。
- 新建 `src/api/flow.ts`：`FlowNode/FlowEdge/FlowSaveDTO/FlowResult` + `flowAPI.save`(POST /api/flow/save)、`get`(GET /api/flow/get?flowGraph=)。**约定接口，后端待实现；编辑态本地可用。**
- 误建的 `src/components/flow/design.vue` 及对应路由/菜单入口已删除，统一由 workflow/draw 承接。

## flow.ts 补节点/连线接口 + 合并 graph.ts（2026-08-28）
- `flow.ts` 新增 `FlowNodeSaveDTO`/`FlowEdgeSaveDTO`(= Omit<...,'id'> & {id?})、`flowNodeAPI`、`flowEdgeAPI`：list(GET /api/flowNode|flowEdge/list?flowGraph=)、save(/save)、update(PUT /update)、remove(DELETE /:id)、saveBatch(/batch)。命名风格同 `/api/flowGraph/*`，后端未实现。
- **`src/api/graph.ts` 已删除**，其 `GraphRow/GraphSaveDTO/GraphSearchParams/GraphSearchResult` 与 `graphAPI`（实际路径为 `/api/flowGraph/search|save|update|{id}`，非旧的 /api/graph/*）迁入 `flow.ts`；`flowAPI` 新增 `graph: graphAPI`。
- 唯一引用方 `src/components/workflow/flow.vue` 的 import 改为 `../../api/flow`；全仓已无 `api/graph` 引用。
- `src/components/approval/graph.vue`（审批流程图查看页）不依赖 api/graph，未受影响。
- **流程相关接口一律从 `src/api/flow.ts` 导入**（flowAPI / flowNodeAPI / flowEdgeAPI / graphAPI）。

## flowAPI.save/get 可删性核查（2026-08-28，仅核查未改码）
- `flowAPI.save`：唯一调用方是 `src/components/workflow/bak.vue`（**孤儿备份文件，router/组件均无引用**）→ 可安全删除，能力已被 `flowNodeAPI.saveBatch` + `flowEdgeAPI.saveBatch` 覆盖。
- `flowAPI.get`：**不可直接删**，`src/components/workflow/draw.vue`（线上页）的 `load()` 依赖它一次性取 nodes+edges；如需删除须先把 load 改为并行 `flowNodeAPI.list` + `flowEdgeAPI.list` 再合并。
- `draw.vue` 目前只有 load，尚未实现保存功能。

## 删除 flowAPI.save（2026-08-28）
- `flowAPI.save`（原 POST /api/flow/save）已删除；`flowAPI.get` 保留。
- 同步改 `src/components/workflow/bak.vue`（孤儿备份文件，无路由引用）：import 去掉 `FlowSaveDTO`，onSave 改为 `Promise.all([flowNodeAPI.saveBatch(fg, nodes), flowEdgeAPI.saveBatch(fg, edges)])`。
- 注意 tsconfig.app.json 开了 `noUnusedLocals`，删接口后必须同步清理未使用的 type import，否则 `npm run build` 报错。
- `flow.ts` 中 `FlowSaveDTO` 类型已随之删除（全仓无引用）。

## 强制删除 flowAPI.get + 删除 bak.vue（2026-08-28）
- `flowAPI.get`（GET /api/flow/get）已删除，随之删除已无引用的 `FlowResult` 类型；`flowAPI` 现为纯聚合器 `{ node, edge, graph }`，无自有接口。
- `src/components/workflow/draw.vue` 的 `load()` 改为 `Promise.all([flowNodeAPI.list(fg), flowEdgeAPI.list(fg)])` 分别取节点/连线，原「一次性取整图」逻辑废弃；import 由 `flowAPI` 改为 `flowNodeAPI, flowEdgeAPI`。
- `src/components/workflow/bak.vue` 已删除（孤儿备份文件）。
- 至此 `/api/flow/*` 系列接口全部移除，流程侧仅剩 `/api/flowGraph/*`、`/api/flowNode/*`、`/api/flowEdge/*` 三组约定接口。

## draw.vue 加载数据源确认 + list 接口改 query 参数（2026-08-28）
- 用户确认思路：**draw.vue 通过路由 query 的 flowGraph 加载节点与边线**，即 `Promise.all([flowNodeAPI.list(fg), flowEdgeAPI.list(fg)])`（mounted 加载、flowGraph 变化时清空重载、为空时不加载并提示）。
- `flowNodeAPI.list` / `flowEdgeAPI.list` 参数位置调整：路径参数 `GET /api/flowNode/flowGraph/{fg}` → **query 参数 `GET /api/flowNode/list?flowGraph=xx`**（flowEdge 同理），`http.get(url, { params: { flowGraph } })`；调用方签名不变，仍是 `list(flowGraph: string)`。
- 遗留：左侧菜单「流程图设计」直达 `/web/workflow/draw` 不带 query，此时页面提示「未传入 flowGraph 参数」且不加载；后端 `/api/flowNode/*`、`/api/flowEdge/*` 仍未实现。

## draw.vue 表格改只读 + 弹窗编辑（2026-08-28）
- 用户指令「所有的表格行是只读的」。按项目既有约定（`workflow/flow.vue`：表格只读 + 弹窗新增/编辑）实现。
- 节点表/连线表全部列改为纯文本展示（`prop` + `show-overflow-tooltip`），移除行内 `el-input`/`el-select`/`el-color-picker`；分类显示 label（`optionLabel`），fromNode/toNode 显示节点名（`nodeLabel`），连线颜色用 `.color-dot` 色块。
- 操作列改为「编辑 + 删除」；新增按钮仍在区块头。新增 `shapeOptions`（rect/round/diamond/circle）。
- 新增节点/连线两个 `el-dialog`（`nodeForm`/`edgeForm` + `nodeRules`/`edgeRules`，`editingNodeIndex`/`editingEdgeIndex` 为 null 即新增），弹窗内含行内表格未暴露的 `shape`/`axis`/`color` 字段。
- 改动仅本地状态，**未接后端保存**（后端 `/api/flowNode/*`、`/api/flowEdge/*` 未实现）。
