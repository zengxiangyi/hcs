# 2026-08-18

## 保护目录
- `.codebuddy/rules/FileProtection.mdc` 目录保护新增 `public`、`node_modules`、`.vscode`（含 `.idea` 共 4 个）。

## 路由鉴权
- `router.beforeEach`：白名单 `WHITE_LIST = new Set(['Login'])`（按路由 name）；非白名单先查 `localStorage.token`，再调 `baseAPI.getUserInfo()` 校验，失败清 token 重定向 `/` 并带 `query.redirect`。

## 登录密码 MD5
- 新增 `src/utils/md5.ts`（`md5(str)`，支持 UTF-8/中文）；前端 MD5 后提交，后端直存直比。
- 契约定稿：**HTTP 401 = token 失效**（清 token + 跳 `/`）；业务失败用 400 不用 401（曾因登录失败返 401，被拦截器当 token 失效整页跳转，覆盖登录逻辑）。

## 文档与清理
- 新建/重写 `CODEBUDDY.md`、`docs/struct.md`、`README.md`。
- 删除模板遗留 `HelloWorld.vue`、`data1.vue`、`src/assets/*`、`public/icons.svg`，同步 router / menu.json / components.d.ts。
- `style.css` 精简为 `:root` 基础字体 + `body{margin:0}`；`base.ts` 新增 `logout()`；Web.vue 顶栏用户名改调 `getUserInfo()`（优先 username，兜底 name）。

## ESLint（加后又删）
- 曾新增 `eslint.config.js` + `lint` 脚本 + 4 个依赖并修正过 `--ext` 在扁平配置下失效的问题；后因 `npm run lint` 报错，用户要求整体删除。`package.json` 与 lock（5225→3951 行）已同步清理。**当前项目无 lint 脚本。**

## 四组件并行评审（data2 / Login / MenuBar / Web）
- 修正 agent 误改：Login 密码不再 trim（只做空校验）；verifyIdentity/resetPassword 提交时才 trim。
- 关键改动：data2 改 `await formRef.validate()`、隐藏 id 改独立 `editId` ref、抽 `buildQueryParams`、`ALL_PAGE_SIZE=99999`、导入统计成败条数；Login 移除遗留 `debugger;`、补邮箱正则 + 密码 ≥6 位 + 两次一致校验；MenuBar 用 `useRoute()` 高亮并自动展开祖先链、补无障碍属性；Web 改 `useRouter()`、移除 `::global(#app)` 侵入样式。
- 各组件补 `defineOptions({ name })`。验证：lint 0、`vue-tsc --noEmit` 通过、`npm run build` 成功（仅既有 chunk 体积警告）。
