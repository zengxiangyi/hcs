# 2026-08-20

## RBAC（用户-角色-权限）系统 — 初版基于 mock
- 新增 `src/components/sys/mock.ts`（users/roles/rights + userRoleMap/roleRightMap 关联表，含 getRightCodesByUser 等）与 `permission.ts`（`setCurrentUser(userId)` / `hasRight(code)` / `restoreCurrentUserRights()`）。
- 新增 5 个页面：`user.vue`、`role.vue`、`right.vue`（按钮权限用 hasRight 控制禁用）、`roleUser.vue`（选角色→勾选用户）、`roleRright.vue`（选角色→按模块分组勾选权限）。
- 打通链路：router 注册 5 个 `/web/hcs/sys/*`（`meta.right` 页面权限 + 守卫校验，无权限回跳 Web 带 noAuth）；MenuBar `filterMenu` 按 right 过滤；menu.json 子项加 right；Login 登录后按 username 匹配 mock 调 setCurrentUser；Web.vue 监听 `route.query.noAuth` 提示。

## 后续状态（已被覆盖）
- `mock.ts` 于 08-26 彻底删除，`permission.ts` 重写为接收后端 rights 数组；系统管理菜单/路由的 right 也已临时移除。此文件内容仅作历史沿革保留。
