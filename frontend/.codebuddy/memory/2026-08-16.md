# 2026-08-16 工作日志

## 项目文档与配置
- 建立 `docs/struct.md`：.gitignore 逐行解析、根目录文件说明、技术栈版本（vue3.5.40/vite8.2.0/element-plus2.14.4/pinia4.0.3/vue-router5.2.0/axios1.19.0/xlsx0.18.5）、运行命令、项目结构。
- 生成 `CODEBUDDY.md` 项目指南（此前无任何 AGENTS 类文件）。

## 前端页面与路由
- 创建 `Login.vue`（居中表单：用户名/密码/确定，浅蓝背景、白底输入框、橙黄按钮、按钮与输入框同宽）；用 `:global(#app)` 覆盖全局 `#app` 1126px 宽限制使背景铺满。
- 创建 `main.vue`（左右布局，左 aside 20% #cecece，右 main 80% #fff，100vh 铺满）。
- 配置 `src/router/index.ts`（`/`→Login，`/main`→main，懒加载）；`App.vue` 改渲染 `<router-view/>`。
- 创建 `MenuBar.vue`：加载 `src/config/menu.json` 树形菜单，子菜单默认隐藏，`expandeds: Set<string>` 记录展开父级，叶子节点 `router.push`。
- 子路由 path 必须用相对路径（去掉开头 `/`）才能嵌入父路由的 `<router-view/>`。
- `Web.vue` 右侧 `<main>` 内嵌 `<router-view/>`；顶栏 `.title` 用 flex + space-between，`left`/`right` 两端对齐；退出按钮改 Element Plus `el-button`。
- 删除 `Placeholder.vue`（无引用）并清理 `components.d.ts` 残留声明。

## 前端 API 层（前端部分约定）
- `http.ts` 新增 `ApiResponse<T>`；请求拦截注入 `Authorization: Bearer <token>`；响应拦截按 `code!==200` 抛错并解包 data，`401` 时清 token 并跳 `/`。
- 新增 `TypedHttp` 接口，各方法 `<T>() => Promise<ApiResponse<T>>`；约定 `res.data` 即业务数据 T。
- `base.ts` 登录/用户信息接口；`data.ts` 用户 CRUD（getUsers/addUser/updateUser/deleteUser）。
- `data2.vue` 由本地 mock 改服务端分页+过滤（keyword/dept/status），调 `dataAPI.getUsers`。
- `vite.config.ts`：`/api` 代理到 `http://localhost:8080`。

## 遗留/注意
- 工作区根目录规则：AI Agent 禁止修改 `.idea/`、`script/`、`config/`（可读不可写）。
- 每次回复末尾需输出任务状态摘要。
- IDE ts-server 存在陈旧诊断缓存问题：跨文件类型引用更新后，lint 报错行号/内容可能不刷新（文件系统代码实际正确），需重载 TS 服务。
