# 2026-08-16

## 项目文档
- 建 `docs/struct.md`（.gitignore 解析、结构、技术栈、命令）；生成 `CODEBUDDY.md`（此前无任何 AGENTS 类文件）。

## 骨架搭建
- 新增 `Login.vue`（居中表单，`:global(#app)` 覆盖模板 1126px 宽限制）、`main.vue`（左 20% / 右 80%，100vh）。
- `router/index.ts`：`/`→Login、`/main`→main，懒加载；`App.vue` 只渲染 `<router-view/>`。
- `MenuBar.vue`：读 `src/config/menu.json` 递归渲染，`expandeds: Set` 管展开，叶子 `router.push`。
- `Web.vue`：右侧 `<main>` 内嵌 `<router-view/>`，顶栏 flex + space-between 两端对齐，退出改 `el-button`。
- 删除无引用的 `Placeholder.vue` 并清理 `components.d.ts` 残留。

## API 层
- `http.ts` 新增 `ApiResponse<T>`、Bearer 注入、`code!==200` 抛错并解包 data、401 清 token 跳 `/`；导出 `TypedHttp`。
- `base.ts`（登录/用户信息）、`data.ts`（用户 CRUD）；`data2.vue` 由本地 mock 改服务端分页 + 过滤。
- `vite.config.ts` `/api` 代理到后端。

## 约定
- 工作区禁止修改 `.idea/`、`script/`、`config/`；回复末尾输出任务状态摘要。
- IDE ts-server 存在跨文件陈旧诊断，需重载 TS 服务。
