# 2026-08-31

## approval/done.vue 隐藏 ID 列
- `src/components/approval/done.vue` 表格首列 `<el-table-column prop="id" hidden label="ID" width="80" />` 使用了 `hidden`，Element Plus 无此 prop，列仍会渲染。已直接删除该列（`row.id` 通过插槽 row 获取，不影响「退回/查看」操作）。
- 约定：Element Plus 表格隐藏列只能用 `v-if`（条件渲染），不要写 `hidden`。
- 遗留问题（未处理）：
  - `done.vue` 的 `buildQueryParams` 已声明但未被 `fetchData` 使用（ts 警告 6133）。
  - `done.vue` 操作列按钮文案与行为不一致：「退回」调用 `handleApprove`（通过接口），「查看」调用 `handleReject`（驳回接口）；`todo.vue`、`send.vue` 同样存在此问题。
  - `done.vue` 缺少 `todo.vue`/`send.vue` 中的 `<el-table-column type="index" label="序号">` 列。
