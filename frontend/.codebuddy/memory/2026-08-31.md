# 2026-08-31

## approval/done.vue 隐藏 ID 列
- `src/components/approval/done.vue` 表格首列 `<el-table-column prop="id" hidden label="ID" width="80" />` 使用了 `hidden`，Element Plus 无此 prop，列仍会渲染。已直接删除该列（`row.id` 通过插槽 row 获取，不影响「退回/查看」操作）。
- 约定：Element Plus 表格隐藏列只能用 `v-if`（条件渲染），不要写 `hidden`。
- 遗留问题（未处理）：
  - `done.vue` 的 `buildQueryParams` 已声明但未被 `fetchData` 使用（ts 警告 6133）。
  - `done.vue` 操作列按钮文案与行为不一致：「退回」调用 `handleApprove`（通过接口），「查看」调用 `handleReject`（驳回接口）；`todo.vue`、`send.vue` 同样存在此问题。
  - `done.vue` 缺少 `todo.vue`/`send.vue` 中的 `<el-table-column type="index" label="序号">` 列。

## 清理 components.d.ts 无效声明
- 删除了 12 条指向不存在文件的声明：`Bak`(workflow/bak.vue)、`Closed`(approval/closed.vue)、`copy`(workflow/draw copy.vue)、`Data1`/`Data2`(data/ 目录已整体移除)、`Dbd`(hcs/product/)、`Design`(flow/design.vue)、`Graph`(approval/graph.vue)、`Job`(hcs/process/)、`Plan`(hcs/product/)、`Process_h`(process/process_h.vue)、`SandPlan`(tech/plan/)。
- 27 个 `El*` 声明 + `vLoading` 指令经与模板实际 `el-*` 标签逐一比对，全部在用，保留；`El*` 与 `vLoading` 不计入清理范围。
- 已确认 `src/router/index.ts` 与 `src/config/menu.json` 均无对被删组件的引用。
- 注意：`components.d.ts` 是 `unplugin-vue-components`（vite.config.ts 中 `dts: true`）自动生成产物，重启 dev server 会按实际文件重新生成；已删除项因源文件不存在不会再回来。
