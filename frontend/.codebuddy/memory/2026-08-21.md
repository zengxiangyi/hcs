# 2026-08-21

- 创建 `src/components/product/transfer.vue`：上方调拨单表格（含查询/重置按钮、行选中、分页），下方蓝本信息表格（含绑定按钮）。沿用 data2.vue 的 ElementPlus 写法。数据为本地 mock，已标注 `productAPI.getTransfers/getBlueprints/bindBlueprint` 接入点（后端接口未提供）。路由 `/web/product/transfer` 已配置。
