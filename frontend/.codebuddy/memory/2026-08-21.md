# 2026-08-21

## product/transfer.vue 初版
- 上方调拨单表格（查询/重置、行选中、分页）+ 下方蓝本信息表格（含绑定按钮），沿用 data2.vue 的 Element Plus 写法。
- 数据为本地 mock，标注了 `productAPI.getTransfers / getBlueprints / bindBlueprint` 接入点（当时后端未提供）。
- 路由 `/web/product/transfer` 已配置。

## 后续状态（已被覆盖）
- 08-28 已移除 mock，改为 `src/api/transfer.ts` 的 `transferAPI.search / create / bindBlueprint` + `blueprintAPI.search`。
