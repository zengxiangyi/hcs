# 2026-08-23

## sys 模块字段对齐 table.md
- 以 `docs/table.md` 为权威字段来源，对齐了 `src/components/sys/` 下 5 个 Vue 页面与 `mock.ts`。
- User: `username→code`(编号)、`nickname→name`(名称)、`dept→department`(部门)，新增 `position/cellphone/email/remark/password` 字段。
- Role: 新增 `category`(分类) 字段。
- Right: `module→category`(分类)，移除 `type`(page/btn) 维度；分组改为按 category。
- 关联页 roleUser/roleRright 同步用新字段名；permission.ts 的 isAdmin 改判 `u.code === 'admin'`。
- table.md 在 SysUser/SysRole/SysRight 表后补充了“前端字段映射”说明。
- 采用多 agent 并行：mock.ts+permission.ts 由主线程改，4 个 agent 分别改 user/role/right/两个关联页。最终 lint 0 错误，无旧字段残留。

## 新增 constValue 常量值管理页面
- 参照 `sys/role.vue` 模式新增 `src/components/sys/constValue.vue`（含 mark 字段，支持 code/name/category 三条件查询）。
- `mock.ts` 新增 `ConstValue` 接口与 5 条示例数据；新增权限 `page:constValue`(id15) + `btn:constValue:add/edit/delete`(16-18)；访客角色(3)追加页面权限 15，管理员自动全量。
- 路由 `web/sys/constValue`（meta.right=page:constValue）；菜单「系统管理 → 常量值管理」（icon=Collection）。
- `docs/table.md` constValue 表补充前端映射说明。

## constValue 操作权限改为 API 下发（非前端固定）
- 需求：常量值页面的 增/改/删 操作权限后期由后端 API 返回，不再前端写死。
- `src/api/base.ts` 新增 `getUserButtons(pageCode)` 接口（GET `/api/rights/buttons?pageCode=`，返回该页面按钮权限码数组 `UserButtons`）。
- `src/components/sys/constValue.vue` 改为 `onMounted` 调用 `loadPageButtons()` 拉取按钮集合，`canAdd/Edit/Delete` 优先以接口返回集合为准；接口未接入/失败时静默回退到 `hasRight`（保留 demo 可用，接入后端后可移除兜底）。
- 全局权限（菜单/路由守卫）仍依赖 `permission.ts` 的 mock，本次未改动。

## constValue 临时开发默认开放权限
- 用户要求临时开发阶段直接放开操作权限。将兜底从 hasRight 改为：新增 `rightsReady` 标志，接口未成功返回前 `canAdd/Edit/Delete` 默认 `true`（临时开放）；接口返回后严格按 `pageButtons` 判断。
- 移除了 constValue.vue 中对 `hasRight` 的 import（不再使用）。
