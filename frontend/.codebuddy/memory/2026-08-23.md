# 2026-08-23

## sys 模块字段对齐 docs/table.md
- 以 `docs/table.md` 为权威字段来源，对齐 `src/components/sys/` 下 5 个页面与 `mock.ts`：
  - User：`username→code`(编号)、`nickname→name`(名称)、`dept→department`，新增 position/cellphone/email/remark/password。
  - Role：新增 `category`(分类)。
  - Right：`module→category`，移除 type(page/btn) 维度，分组改按 category。
- `roleUser.vue` / `roleRright.vue` 同步新字段名；`permission.ts` 的 isAdmin 改判 `u.code === 'admin'`。
- 多 agent 并行：主线程改 mock.ts + permission.ts，4 个 agent 分别改 user/role/right/两个关联页。最终 lint 0，无旧字段残留。

## constValue 常量值管理页
- 新增 `sys/constValue.vue`（含 mark 字段，支持 code/name/category 三条件查询）；mock 增 5 条示例数据 + 权限 `page:constValue`、`btn:constValue:add/edit/delete`。
- 路由 `web/sys/constValue`（meta.right=page:constValue）；菜单「系统管理 → 常量值管理」。

## 按钮权限改为 API 下发
- `src/api/base.ts` 新增 `getUserButtons(pageCode)`（`GET /api/rights/buttons?pageCode=`，返回按钮权限码数组）。
- `constValue.vue` 在 `onMounted` 拉取 `pageButtons`，`canAdd/Edit/Delete` 以接口返回为准。
- 临时开发放开：新增 `rightsReady` 标志，接口未成功返回前 `canAdd/Edit/Delete` 默认 true；返回后严格判断。同时移除对 `hasRight` 的 import。
