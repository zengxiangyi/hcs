# 2026-08-17

## data2.vue Excel 导入导出
- 导出：pageSize=99999 拉全量 → `XLSX.utils.json_to_sheet` + `book_new` + `writeFile`。
- 导入：`el-upload`（auto-upload=false, accept .xlsx/.xls），`file.arrayBuffer()` + `XLSX.read` + `sheet_to_json` 按中文表头解析，逐行 `dataAPI.addUser` 后刷新。
- 注意：图标须显式 `import { UploadFilled } from '@element-plus/icons-vue'`（unplugin 不解析图标）。

## 中文化
- `main.ts`：`app.use(ElementPlus, { locale: zhCn })`，全局生效。

## 忘记密码（两步弹窗）
- ①手机号 + 邮箱校验匹配 ②新密码 + 确认密码重置；不匹配用 `ElMessageBox.alert`。
- `base.ts` 新增 `verifyIdentity` / `resetPassword`。
- http 错误拦截器增强：非 2xx 时解包 `{code,msg}` 以 `Error(msg)` reject，使业务文案可被 catch 读取。
- `Login.vue` 登录失败改 `ElMessageBox.alert(err?.message || ...)`。
- 用户并行重构 users 表字段（name→userName、role→roleName、dept→department、status→state），前端已同步（08-23 再对齐为 code/name/department/state）。

## 约定
- 后端实现细节不写入前端记忆，只记前端工作 + 跨端契约。
