# 2026-09-01

## approval/send.vue 查看按钮跳转流程实例页
- `seeProcess(row)` 改为 `router.push({ name: 'Instance', query: { workflow: row.code } })`，移除原驳回逻辑，导入 `useRouter`。
- 关键点：路由 `Instance`（`src/router/index.ts` path `workflow/instance`）未声明动态段，`instance.vue` 通过 `route.query.workflow` 取值，因此必须用 `query` 而非 `params`，否则刷新丢参。
- 模板"查看"按钮绑定由 `handleReject`（不存在）修正为 `seeProcess`；"退回"仍绑定 `handleApprove`（调 `/api/workflow/{id}/approve`）。
- 遗留：`buildQueryParams` 未使用告警（既有问题，未处理）。

## workflow/instance.vue 纯 CSS 布局调整
- 在 `<style scoped>` 内新增规则，未改动 template/script：
  - `:deep(.el-table__body-wrapper)` 与 `:deep(.el-scrollbar__wrap)` 设 `min-height: 96px` / `max-height: 300px`，表格默认高度受限，超出后表体滚动、表头固定。
  - `#flowmap`（底部 canvas）设 `display: block; margin: 0 auto; position: relative; max-width: 100%`，实现居中；`position: relative` 同时让 `v-loading` 遮罩正确覆盖。
- 注意：canvas 的 width/height 由 JS 内联设置，内联样式优先级高于 scoped CSS，故只限制 `max-width` 不覆盖 `height`。

## approval/send.vue 首次加载无数据修复
- 根因：`<script setup>` 顶部已 `import { onMounted }`，但文件末尾漏写 `onMounted(fetchData)`，导致进入页面不发起 `/api/workflow/sender` 请求，只有手动点查询/重置才有数据。
- 修复：在 `seeProcess` 之后补 `onMounted(fetchData)`，与同级 `approval/todo.vue`(行177)、`approval/done.vue`(行176) 写法保持一致。
- 同类页面均已在 script 末尾调用 `onMounted(fetchData)`，新增列表页时务必检查，不要只 import 不用。
- 遗留：`buildQueryParams` 仍未被使用（ts-plugin 6133 警告，既有问题，未处理）。
