# 2026-08-26

## tech/plan 组件
- `SandPlan.vue` → `TZ01.vue`（内部 name 已是 TZ01），同步重命名 `SandRow→TZ01Row`、类名 `sand-plan→tz01-plan`，board.vue 映射改 `sand: TZ01`。**约定：方案组件统一 `TZxx.vue`，注册键 = craftTree 二级工艺 value。**
- TZ01 精简为单个动态表格（段号/温度/时间/备注），删除工艺参数表单与其他列。
- 修复「新增一行」无反应：根因 `rows` 被定义为只读 `computed(() => model.value.rows)`，push/splice 不触发更新 → 改为 `reactive(model.value.rows)`（复用原引用，不破坏 defineModel 双向绑定）。
- 增强：`insertRow(index)` 指定位置插入；`removeRow` 加 ElMessageBox 二次确认，仅剩一行禁删。**约定：方案表格数组勿用只读 computed。**

## board.vue 修正
- 一/二级工艺 select 的 v-model 由失效的 `firstLevel`/`secondLevel` 对齐到 `basicForm.firstLevel/secondLevel`；删除多余 ref，联动逻辑统一改用它。
- 修复拼写错误 `requirementForm.hardendeep → hardnessDepth`。

## 接入后端（去 mock）
- 新建 `src/api/tech.ts`：`techAPI.save(data)` → `POST /api/tech/board`；`board.vue` 的 onSave 改 async + ElMessage，移除 `debugger;` 与 TODO。
- `sys/user.vue` 列表/增删改全量改接 `src/api/data.ts` 的 `sysUserAPI`（`GET /api/sysUser/list`、`POST/PUT/DELETE /api/sysUser`），服务端分页 + `v-loading`。
- **彻底删除 `src/components/sys/mock.ts`**（用户明确要求），迁移清单：
  - `role.vue`→roleAPI、`right.vue`→rightAPI、`roleUser.vue`→roleAPI+dataAPI+roleUserAPI、`roleRright.vue`→roleAPI+rightAPI+roleRightAPI（关联键 id→rightCode）、`constValue.vue`→constValueAPI（新增 `src/api/constValue.ts`）。
- `permission.ts` 重写：移除 mock 依赖；`setCurrentUser(rightCodes: string[])` 直接接收后端权限码集合；删除 getRightCodesByUser/users/isAdmin/getCurrentUserId。
- 关联保存：roleUser 走 `roleUserAPI.save`（全量替换 userIds）；roleRright 走 `roleRightAPI.save`（全量替换 rightCodes）；`dataAPI.list({pageSize:99999})` 拉全量用户供勾选。
- 注意：项目无 `@` 别名，API 文件用相对路径 import；sysUser 的增删改路径是按 REST 约定推断的，若后端实际不同需调整。

## 系统管理菜单/路由权限放开
- 根因：系统管理 5 个子菜单都带 `right` 字段，而登录从未调用 `setCurrentUser()`（localStorage rights 为空）→ `filterMenu` 全部过滤 → 父级无子项整体隐藏。
- 用户选「所有用户可见」：menu.json 移除 5 个子菜单的 right；router 同步移除 5 个 sys 路由的 `meta.right`。**约定：菜单显隐与路由拦截需成对处理。**

## sys 六文件复核修复
- `roleUser.vue`：修 2 处编译错误（`buildTable` 访问不存在的 `u.role`）+ 字段错配（模板用 username/role/dept/status）；改用 `sysUserAPI` / `SysUserRow`，删除无效 buildTable/roleNames，列改 code/name/department/state。
- `right.vue`：补齐 hasRight 按钮权限（canAdd/canEdit/canDelete）。
- `roleRright.vue`：权限分类标题中文化（page→页面、button→按钮），`groupedRights` 增 label 字段。
- user.vue / role.vue / permission.ts 复核无逻辑错误。

## 临时移除按钮权限（测试阶段）
- 按用户要求从 `user.vue`、`role.vue`、`right.vue` 移除全部 hasRight 逻辑（import、computed、handler 拦截、模板 :disabled）。
- `permission.ts` 保留（router/MenuBar 仍在用）。属临时改动，恢复权限控制时重新加回即可。

## 环境提示
- IDE TS/Volar 对 `roleRright.vue` 存在陈旧诊断缓存（指向已删除的 `checkedRightIds`、旧 mock 类型）；grep 已确认无残留，需用户重启 TS Server。已把内联 some 抽成 `groupIndeterminate(group)` 降低陈旧解析概率。
