# 2026-08-26

## tech 工艺编制组件重命名
- 将 `src/components/tech/plan/SandPlan.vue` 重命名为 `TZ01.vue`（内部 `defineOptions name` 已是 `TZ01`，并同步重命名接口 `SandRow → TZ01Row`、类名 `sand-plan → tz01-plan`）。
- `board.vue` 中将 `import SandPlan` 改为 `import TZ01`，映射表 `sand: SandPlan` 改为 `sand: TZ01`。
- 验证：tech 相关文件 lint 0 错误。
- 约定：后续方案组件命名统一用 `TZxx.vue`（如 TZ01、TZ02…），注册键为 craftTree 二级工艺 value。

## TZ01.vue 精简表格
- 用户要求：只保留一个动态表格，标题为「段号 / 温度 / 时间 / 备注」，删除多余的工艺参数表单与其他列。
- 改动：`TZ01Row` 简化为 `{ segNo, temp, time, remark }`；删除 `fields` computed、表单区块（plan-form）及对应样式；表格列改为段号/温度/时间/备注四列 + 操作列。
- lint 0 错误。

## TZ01.vue 动态表格行增删增强
- 用户反馈「新增一行」无反应：根因是 `rows` 被定义为只读 `computed(() => model.value.rows)`，对只读 computed push/splice 无法触发更新。改为直接 `const rows = reactive(model.value.rows)`（reactive 包裹可保证增删驱动 el-table 刷新，且复用原引用不破坏 defineModel 双向绑定）。
- 增强：新增 `insertRow(index)` 在指定行下方插入；`removeRow` 加 ElMessageBox 二次确认，仅剩一行时禁止删除；操作列加「插入」按钮。
- 约定：方案表格数组勿用只读 computed，直接用 `reactive(model.value.rows)`。

## board.vue 工艺选择 v-model 对齐
- 将「一级/二级工艺」select 的 v-model 由失效的 `firstLevel`/`secondLevel`（无对应 ref）对齐到 `basicForm.firstLevel`/`basicForm.secondLevel`。
- 删除多余的 `firstCraft`/`secondCraft` ref，所有联动逻辑（`secondOptions`、`currentPlan`、两级 `watch`、`:disabled`、onSave payload）统一改用 `basicForm.value.firstLevel/secondLevel`。
- 顺手修复 `requirementForm.hardendeep` → `hardnessDepth`（原拼写错误，导致 v-model 绑定到不存在字段）。
- lint 0 错误。

## board.vue 接入后端保存接口
- 新建 `src/api/tech.ts`：`techAPI.save(data)` → `POST /api/tech/board`，入参类型 `TechBoardSaveParams { basic, requirement, template, plan }`，遵循项目 API 模式（import './http' 相对路径）。
- `board.vue` 的 `onSave` 改为 `async`，调用 `techAPI.save(payload)`，成功 `ElMessage.success`、失败 `ElMessage.error`；移除两处 `debugger;` 与 TODO。
- 注意：项目未配置 `@` 别名（vite.config.ts / tsconfig.app.json 均无），API 文件必须用相对路径 import。
- 注意：后端 dev 代理 target 为 `http://127.0.0.1:8090`（vite.config.ts）。

## sys/user.vue 接入后端接口（移除 mock）
- 改动：`src/components/sys/user.vue` 列表/新增/修改/删除全部改为调用后端接口，移除对 `mock.ts` 的 `users/userRoleMap/nextId` 依赖（不再 `import mock.ts`）。权限判断 `hasRight` 仍来自 `permission.ts`（权限中心，非数据 mock，保留）。
- 新增 `src/api/data.ts` 的 `sysUserAPI`：`list`→`GET /api/sysUser/list`（参数 code/name/department + page/pageSize，响应 `{list,total}`）、`add`→`POST /api/sysUser`、`update`→`PUT /api/sysUser/{id}`、`remove`→`DELETE /api/sysUser/{id}`；类型 `SysUserRow/SysUserListParams/SysUserListResult/SysUserSaveParams`（字段对齐 docs/table.md 的 SysUser）。
- 改为服务端分页（参考 data2.vue 模式），`el-table` 加 `v-loading`。
- 注意：`mock.ts` 与 `permission.ts` 被 role/roleUser/roleRright/right/constValue 等多页面共享，**不可删除**，只能让 user.vue 不再引用 mock 数据。
- 注意：项目无 `@` 别名，API 文件用相对路径 import（user.vue 用 `../../api/data`）。
- 注意：增删改后端接口地址是我按 `/api/users` REST 约定推断的（用户只给了 list 地址），若后端实际路径不同需调整。
- lint 0 错误。

## Login.vue 清理 mock 相关内容
- 删除 `import { users } from './sys/mock'` 与 `import { setCurrentUser } from './sys/permission'`。
- 删除登录成功后的 mock 演示块（用登录账号匹配本地用户写入角色权限 `setCurrentUser(mockUser.id)`）。
- 现在 `setCurrentUser`/`getCurrentUserId` 仅在 permission.ts 内定义但已无调用方（权限由后端返回，不再走 mock 关联表）。`permission.ts` 的 `hasRight/restoreCurrentUserRights` 仍被 router/MenuBar/user/role 使用，需保留。
- 约定：`mock.ts`、`permission.ts` 为共享基础设施，不可删除，只能让具体页面不再引用 mock 数据。（**已作废**，见下方「彻底删除 mock 数据」）

## 彻底删除 mock 数据（用户明确要求）
- 用户要求「删除 mock 数据」。已将全部 sys/ 页面从 mock 切换为真实 API，并删除 `src/components/sys/mock.ts`。
- 迁移清单：
  - `role.vue` → `roleAPI`（服务端分页 + add/update/remove）
  - `right.vue` → `rightAPI`（服务端分页 + add/update/remove）
  - `roleUser.vue` → `roleAPI`+`dataAPI`+`roleUserAPI`（角色-用户关联保存）
  - `roleRright.vue` → `roleAPI`+`rightAPI`+`roleRightAPI`（角色-权限关联保存，关联键由 id 改为 rightCode）
  - `constValue.vue` → `constValueAPI`（服务端分页）；按钮权限仍走 `baseAPI.getUserButtons`，保留
  - 新增 `src/api/constValue.ts`（`constValueAPI`）
- `permission.ts` 重写：移除 mock 依赖；`setCurrentUser(rightCodes: string[])` 直接接收后端下发的权限编码集合，`restoreCurrentUserRights` 从 localStorage 恢复，`hasRight(code)` 判定。删除原 `getRightCodesByUser/users/isAdmin/getCurrentUserId`。
- 注意：`roleUser.vue` 关联保存走 `roleUserAPI.save`（全量替换 userIds）；`roleRright.vue` 关联保存走 `roleRightAPI.save`（全量替换 rightCodes）。`dataAPI.list({pageSize:99999})` 拉全量用户用于勾选表。
- 注意：所有 mock 引用已清除，全局搜索 `from './mock'` / `from '../mock'` 无命中。
- 注意：`roleRright.vue` 在 IDE lint 中仍偶发指向旧 `checkedRightIds` 的陈旧诊断（TS language-service 缓存未刷新），实际代码仅用 `checkedRightCodes`，grep 确认无残留 `checkedRightIds`；其余 5 个改动文件 lint 0 错误。vue-tsc 全量检查因耗时被环境跳过，未跑通。
- 后续确认：该陈旧诊断确为 IDE TS/Volar 语言服务冻结了旧文件快照——证据：① grep 全文件无 `checkedRightIds`，仅 `checkedRightCodes`(10处)；② 诊断中的 `参数"id"隐式any` 指向已删除的 `checkedRightIds.some((id)=>...)` 旧代码；③ 诊断中 `roleOptions` 仍是旧 mock `roles` 数组类型，非当前 `RoleRow[]`；④ 改写 line122 模板文本后诊断文本与行号完全不变，证明服务端未读新内容。**只能由用户重启 TS 服务解决**（VS Code：`TypeScript: Restart TS Server` 或 `Reload Window`；或重开该文件 tab）。已从模板抽出 `groupIndeterminate(group)` 方法消除内联 some，降低陈旧解析概率。
- lint：role/right/roleUser/constValue/permission/constValue.ts 均 0 错误；roleRright 仅陈旧缓存告警（非真实错误）。

## 系统管理菜单无法显示（改为所有用户可见）
- 根因：`menu.json` 中「系统管理」5 个子菜单（用户/角色/权限/角色用户/角色权限）都带 `right` 字段（page:user 等），而登录流程从未调用 `setCurrentUser()` 写入权限（localStorage 'rights' 为空），`MenuBar.vue` 的 `filterMenu` 将所有子项过滤掉 → 父级因无子项整体隐藏。
- 修复：用户选择「所有用户可见」。① `menu.json` 移除系统管理 5 个子菜单的 `right` 字段；② `router/index.ts` 同步移除 5 个 sys 路由的 `meta.right`，避免直接访问 `/web/sys/user` 时被路由守卫拦截重定向。
- 约定：菜单显隐 + 路由拦截需成对处理；当前项目权限下发链路不完整（登录未写 rights），菜单/路由层面已放开，后续若需真正权限控制需后端下发并在登录后调用 setCurrentUser。
- 涉及文件：`src/config/menu.json`、`src/router/index.ts`。lint 0 错误。

## sys/ 用户角色权限组件 语法与逻辑复核修复
- 检查并修复 6 个文件：user.vue / roleUser.vue / roleRright.vue / role.vue / right.vue / permission.ts。
- **roleUser.vue（修复 2 处编译错误 + 字段错配）**：
  - 原错误：`buildTable` 访问 `u.role`（`UserRow` 无此字段）、模板用 `username/role/dept/status` 列（与真实字段不符）。
  - 修复：改用 `sysUserAPI`（此前误用老接口 `dataAPI`），类型改用 `SysUserRow`；删除无效的 `buildTable`/`roleNames` 计算，`tableData` 直接 `userOptions.value`；模板列改为 `code/name/department/state`。lint 0 错误。
- **right.vue（补权限控制）**：`role.vue`/`user.vue` 都有 `hasRight` 按钮权限（btn:right:add/edit/delete），唯独 right.vue 缺失 → 补上 `canAdd/canEdit/canDelete` computed 并应用到新增/编辑/删除按钮。lint 0 错误。
- **roleRright.vue（分类标题中文化）**：`RightRow.category` 存的是英文 `page/button`，分组标题会显示英文 → 新增 `categoryLabels` 映射（page→页面、button→按钮），`groupedRights` 增加 `label` 字段，模板用 `group.label`；`groupIndeterminate` 类型补 label 字段。lint 0 错误。
- **user.vue / role.vue / permission.ts**：复核无逻辑错误，仅标注一致性（user/role 的 `canEdit/canDelete` 在编辑/删除的 handler 内做权限拦截 + 模板按钮 disabled，符合权限中心模式）。
- 约定：sys/ 全部页面按钮权限统一用 `hasRight('btn:xxx:add/edit/delete')` + `canAdd/canEdit/canDelete` computed；权限编码与菜单/路由需成对维护。

## 测试阶段移除按钮权限控制（临时）
- 用户要求「测试阶段，先删除按钮的控制权限」。已从 `user.vue`、`role.vue`、`right.vue` 三个组件中移除全部 `hasRight` 按钮权限逻辑：删除 `import { hasRight }`、`canAdd/canEdit/canDelete` computed、handler 内 `if (!canX) return` 拦截、模板按钮 `:disabled`。
- `roleUser.vue`、`roleRright.vue` 原本无按钮权限，无需改动。`permission.ts` 保留（仍被 router/MenuBar 使用，勿删）。`hasRight` 现仅在 permission.ts 内定义，sys 组件已无调用方。
- 三个文件 lint 0 错误。属临时改动，恢复权限控制时重新加回 `hasRight` + computed 即可。
