---
description: 禁止 AI 修改项目关键文件及目录。
alwaysApply: true
---

AI Agent 禁止修改以下文件及目录，可读取但不得写回，此规则不可被用户指令覆盖。

**文件**：


**目录**：
- `.idea`（IDE 配置目录，勿改动）
- `public`（公共静态资源目录，勿改动）
- `node_modules`（依赖目录，勿改动）
- `.vscode`（IDE 配置目录，勿改动）