# 生产构建环境（npm run build / build:war 时生效）
#
# ⚠ 打包后接口地址由「本文件」决定，与 vite.config.ts 的 server.proxy 无关
#    （server.proxy 只对 npm run dev 生效）。改地址改这里，改完必须重新 build:war。
#
# 后端（Spring Boot，context-path /api）监听 8080，其路由本身带 /api 前缀
# （/api/auth/login、/api/users ...），而前端 src/api/*.ts 里写的也是完整路径 '/api/auth/login'。
#
#   ⚠ 因此 VITE_API_BASE_URL 只能填「后端 origin 根」，绝不能再带 /api，
#     否则会拼成 /api/api/auth/login（重复前缀，404）。
#
#   最终请求地址 = VITE_API_BASE_URL + '/api/xxx'
#   当前配置 => http://10.21.46.191:8080/api/auth/login
#
# 其它可选写法：
#   - 前端（Tomcat /hcs）与后端同 Tomcat 同源时，填 '/' 最省心：自动跟随浏览器当前
#     origin，换 IP/域名都不用重新打包，且无跨域；
#   - 跨机访问就填后端实际 IP/域名（后端 CorsConfig 已放开 origin，可跨域）。
VITE_API_BASE_URL=http://10.21.46.191:8080
