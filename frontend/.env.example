# 环境变量示例文件
# 复制为 .env（或 .env.local）后按需修改，修改后需重启 dev 服务器生效

# API 基础地址。
# 默认不设置（留空 / 删除本行）时，开发环境下 Vite 会把 /api 代理到 http://localhost:8080。
# 如需直连远程后端或独立部署，可设置为后端根地址，例如：
#   VITE_API_BASE_URL=/api          # 走 Vite 代理（默认行为）
#   VITE_API_BASE_URL=http://10.0.0.5:8080   # 直连远程后端
# 该变量在 src/api/http.ts 中通过 import.meta.env.VITE_API_BASE_URL 读取。
#VITE_API_BASE_URL=/api
