# 2026-08-25

## MySQL 连接验证
- 测试了 3 个已配置的 MySQL MCP 连接（均通过 `execute_sql` 工具）：
  - **mysql127**（localhost:3306, root/yi7120699abcDE）：✅ 连接成功，MySQL 8.0.46。可访问库：mysql、information_schema、performance_schema、sys、sakila、world、hcs、hcs_test、erp、page。
  - **mysql186**（10.26.105.187:8082, root/Zhiwei@001, 库 excm）：❌ 连接超时（10060），网络不通，需 VPN 或确认网络。
  - **mysql197**（10.26.8.198:8082, root/LianZhu@!202508, 库 excm）：❌ 连接超时（10060），网络不通。
- 项目 `page` 库含 15 张表：(2)、constvalue(0)、flowcurrent(0)、flowedge(3)、flowgraph(1)、flowhistory(7)、flownode(3)、sysright/sysrole/sysroleright/sysroleuser/sysuser(0)、t_info(8)、users(8)、workflow(4)。
- 注意：MCP 的 `execute_sql` 不支持多语句（`USE page; SHOW TABLES;` 报 "Commands out of sync"），需单条查询；多库查询用 information_schema。
