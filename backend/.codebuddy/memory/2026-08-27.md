# 2026-08-27

## SysUser/SysRole/SysRight/BluePrint 模块代码评审与清理
已完成的无效代码/注释清理（28 个文件）：
- 删除 `SysRightController` 未使用的 `import BluePrintQuery`/`import BluePrint`，并修正误写 "BluePrintQuery" 的注释。
- 删除 `SysRoleController`、`SysRightController` 未使用的 `import RequestParam`。
- 删除 `SysUserQuery`/`SysRoleQuery`/`SysRightQuery`/`BluePrintQuery` 中冗余的 `// 参数`/`// 分页` 注释与双空行。
- 删除 `SysRoleController`/`SysRightController` 的 `search` 方法后多余空行；`BluePrintController.listPaged( page - 1` 多余空格。

注意：`AuthController.java` 报告的 "无法解析 SysRoleService.getByUserId"（line 60）经核实为 **lint 陈旧缓存误报**，磁盘代码中无该调用，亦不在本次 28 文件范围内，未处理。

## 评审发现的待改进建议
- ~~Service.search 参数 `int i`（页索引）命名语义不清~~ → **已实施**：4 个 Service 的 `i`/`page` 统一重命名为 `pageOffset`。
- ~~4 个 Controller 的 `searchByQuery` 中 `query.getPage()==null` 判断冗余~~ → **已实施**：简化为直接 `query.getPage()`/`query.getPageSize()`（DTO 字段已有 `=1`/`=10` 默认值），保留 `page<1||size<1` 边界校验。
- 4 个 Controller 的 `searchByQuery` 分页校验+转换逻辑高度重复（lint 已标 WARNING "重复代码段"）→ **待确认**：建议抽公共方法或基类。
- 4 个 Query DTO 分页字段重复 → **待确认**：可考虑抽取 `PageQuery` 基类。
- SysUser/SysRole/SysRight 的 `save/update` 仅做单表校验，缺少跨表唯一性/级联处理（视业务而定）。

## 验证
- Service 层 lint 0 错误；Controller 层仅剩 `AuthController` 陈旧误报（磁盘无 `getByUserId`）与 `SysUserController` 重复代码段 WARNING（即待确认的 #4）。
