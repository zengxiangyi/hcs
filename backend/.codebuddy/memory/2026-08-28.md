# 2026-08-28

## TransferOrder 模块新建
- 新增调拨单（transferorder）功能模块，遵循双持久化收敛规范（JPA 单表 + MyBatis 动态查询）。
- 文件：
  - entity/TransferOrder.java（id 自增主键，其余 23 列均 String length=100，列名小写无下划线如 materialcode、outprocess）
  - repository/TransferOrderRepository.java（findByState / findByCode 简单单表查询）
  - dto/TransferOrderQuery.java（可变查询条件）
  - mapper/TransferOrderMapper.java + resources/mapper/TransferOrderMapper.xml（query + countByQuery 动态 SQL）
  - service/TransferOrderService.java（save/update/getById/getByCode/delete/search）
  - controller/TransferOrderController.java（/transferorder 下 save/update/get/delete/search/list）
- 编译已通过（mvnw -o compile BUILD SUCCESS）。
- 约定：列名全部小写无下划线，与用户指定一致；XML 中 ORDER BY id DESC。

## TaskProcess 模块列名对齐
- 用户确认 taskprocess 表真实列名为无下划线形式：`auditmessage/auditstate/audittime/audituser/blueprint/createtime/createuser/id/state/step/transfer/updatetime/updateuser`（按此顺序）。
- 已修改：TaskProcessMapper.xml（Base_Column_List 顺序+resultMap 显式映射无下划线列到驼峰字段，因全局 map-underscore-to-camel-case 对无下划线列无效）、Query_Where 条件列名、TaskProcess.java 实体 @Column 全部改为无下划线。
- 约定重申：DDL（含列重命名）由 DBA 执行，AI 仅交付应用层代码与 SQL 文本。
