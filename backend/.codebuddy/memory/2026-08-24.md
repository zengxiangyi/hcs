# 2026-08-24（已精简，稳定约定见 MEMORY.md）

## 7 张 workflow 表 schema 对齐（4 agent 并行）

- **FlowHistory**：删非 schema 字段 `flowNode`；`userCode`→`dealUser`、`opTime`→`dealTime`（属性+列+repo/service/controller 全链路）。
- **FlowMap**：删非 schema 字段 `firstNode`；保留 workflow/width/**height**（拼写保留）/remark。
- **FlowNode**：新增 workflow/operator/role/user 四字段；`role`→`roleList`、`user`→`userList`（避 MySQL 保留字），同步 @Column 与 Service 的 setRoleList/setUserList。
- **workflow→flowGraph 重命名**：FlowEdge 字段+@Column+getter/setter 全改；FlowNode 字段已是 flowGraph 但 getter/setter 仍叫 getWorkflow，本次修正。连带改 `FlowNodeRepository.findByFlowGraphAndCategory`、`FlowEdgeRepository.findByFlowGraphAndFromNode` 及 FlowNodeService/FlowEdgeService/FlowEngine 调用。**Workflow 实体本身无 workflow 属性列，未动**；FlowNodeAction/FlowMap/FlowHistory/FlowCurrent 各自独立的 workflow 属性不在本次范围。
- **FlowEdge**：新增 workflow/category/condition/remark(200) 四字段。
- **flowCurrent / flowNodeAction**：新建 entity+repository+service+controller 全套。flowNodeAction 无唯一 code，save 不校验重复，`nextNode` 列名 `NextNode`。
- **Workflow**：本就对齐，未动。

验证：`./mvnw -q compile` 通过（Git Bash；Windows cmd 无 tail）。IDE 对 FlowHistory 报"无法解析表/列"为重命名后的缓存误报。

## 修复 FlowGraph ORM 映射 WARN（Table 'page.flow_graph' doesn't exist）

- 根因：`@Table(name = "flowGraph")`，MySQL（lower_case_table_names=1）下 Hibernate 把驼峰表名转成 `flow_graph`，真实表是全小写 `flowgraph`。
- 修复：仅改 `@Table` 为 `flowgraph`，字段未动。
- **遗留隐患**：FlowNode/FlowEdge/FlowHistory/FlowCurrent 的 `@Table` 仍写驼峰（flowNode 等），同类问题未修。
- `docs/tb.sql` 建表脚本已过时，与真实库不一致；真实库列名为驼峰（flowGraph/title），以 `DESCRIBE` 实际库为准。
