# 2026-08-22（已精简，稳定结论见 MEMORY.md）

## 改动清单

1. **依赖清理**：删 `spring-boot-starter-data-jdbc`、pom 空标签、Lombok；补 `spring-boot-starter-validation`、`spring-boot-starter-test`。
2. **参数校验**：`Info` 加 `@NotBlank/@Size/@NotNull`，`save` 加 `@Valid`；`createTime` 用 `@CreationTimestamp` 服务端填充，忽略客户端传入的 id/createTime。
3. **统一响应/异常**：新增 `common/ApiResponse`、`PageResult`、`GlobalExceptionHandler`、`ResourceNotFoundException`；后补 `IllegalArgumentException`→400（Account 用户名重复用）。
4. **分页**：`/list` `/all` `/search` 统一支持 `page/size`，返回 `PageResult`；Controller 改构造器注入，移除直接 mapper 调用。
5. **测试**：`InfoControllerTest`（@WebMvcTest）/ `InfoRepositoryTest`（@DataJpaTest）/ `InfoMapperTest`（@SpringBootTest）。后两者需本地 MySQL，配置指向 `page_test` 库；本环境多次跳过构建，主代码已 BUILD SUCCESS。
6. **移除残留 Lombok**：`ApiResponse`、`PageResult` 的 `@Getter` 删除，补只读 getter。
7. **文档精简**：3 agent 并行分析后合并 `architecture.md`/`persistence.md`（后者降为薄约定），`commands.md`/`configuration.md` 去重，`tree.md` 折叠 `.idea/` `.codebuddy/`；按用户要求删除全部测试相关文档与引用（`docs/tests.md` 此前已在 git 删除）。
8. **CORS**：新增 `config/CorsConfig.java` + `app.cors.*` + `application.properties`。细节见 MEMORY.md。
9. **`Info.status` Integer → String**：波及 entity/controller/service/repository/mapper 接口；XML 无需改。注意 `ddl-auto=update` 会把 `t_info.status` 由 int 转 varchar(50)，历史整数数据需确认转换结果。
10. **Account 三层生成**：`AccountRepository`（纯 JPA，无 mapper）/ `AccountService`（save 置空 id + 用户名唯一校验；update 不改 username/password）/ `AccountController`（`/account` 下 list/get/save/put/delete）。
11. **多 agent 代码评审**：6 桶并行评审 16 个 Java 文件，仅出报告未改代码 → `docs/plans/2026-08-22-code-review-report.md`。关键项见 MEMORY.md「待跟进」。

## 遗留风险

- `Account.password` 明文入库且 JSON 回显，未加密。
- `/account/**` 受 `anyRequest().authenticated()` 保护，调用需 JWT。
- 评审 S1/S2/I1/I2 均未修复，修改阶段应先验证 I1（context-path 下 requestMatchers 应为 `/auth/**`）与 I5（`mapUnderscoreToCamelCase` 开关）。
