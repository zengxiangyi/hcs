# 2026-08-20

## md5.ts 代码评审 + 落地两项改进
- 评审 `src/utils/md5.ts`：通过，无 P0/P1 缺陷。
- 新增契约测试 `src/utils/__tests__/md5.test.ts`（node:test + assert，锚定 `md5('123456')==='e10adc3949ba59abbe56e057f20f883e'`，与 seed 一致）。
- `package.json` 增加 `test` 脚本：`tsx --test src/utils/__tests__/*.test.ts`。
- 在 `see.md`（安全说明块、待办、建议第 4 条）与 `README.md`（accounts 表）标注 MD5 为"历史兼容性包袱，非规范做法"。
- 注意：本机 `node_modules` 为空（依赖未装），`npm test`/`tsx` 暂无法实际运行；已用纯 `node` 验证断言逻辑全部通过（ALL_PASS）。待 `npm install` 后可跑 `npm test`。
